<!-- markdownlint-disable MD024 -->
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com), and this project adheres to [Semantic Versioning](https://semver.org).

## [Unreleased]

### Changed

- False negatives are fixed — a violation the plugin used to pass over in silence, whether `--fix` was asked for or not, is now reported:
	- The `selector-descendant-combinator-no-non-space` rule now reads a selector whose comment the parser carries across a parenthesised group (see [#159](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/159)). A group standing where a combinator belongs is nothing `postcss-selector-parser` has a place for: it reads the group as a combinator whose value is the whitespace in front of it and the group itself, and files a comment standing between the two behind that node rather than inside it, so `.foo /*c*/\t( )\t.bar` came back out of the parser as `.foo ( )/*c*/\t\t.bar` — the comment across the group, and a tab where none was written. The rule passed such a selector over, and a list at a time, so a run standing in a neighbouring selector went unreported with it. The node is given its own text back now, so the group is reported where it stands and the whitespace behind it is collapsed like any other descendant combinator's, the comment keeping the place the file gives it. Only a comment with a space in front of it was ever read that way, however wide the whitespace is: that space is what the value of such a combinator opens with, while a comment standing behind a tab or a line break the parser already prints as the file spells it.
	- The `selector-descendant-combinator-no-non-space` rule now reads a selector carrying an inline comment under `postcss-scss` (see [#158](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/158)). That syntax rewrites such a comment into a block comment in the copy the rule reads, keeps the source spelling in the copy it prints, and the two drift apart by two characters per comment, so the rule used to pass the whole selector list over. Every position is now translated back into the file's own coordinates, and a fix is written to both copies with the comments spelled the way the file spells them. The whitespace standing behind an inline comment is passed over on its own: the line break in it is what closes the comment, so a single space could not be written there, and asking for one would leave a warning no run of `--fix` could clear.
	- The `selector-pseudo-class-parentheses-space-inside` rule now reads a selector carrying an inline comment under `postcss-scss` (see [#163](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/163)). It used to pass such a selector over, as [#161](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/161) left it: the two copies that syntax keeps of a selector drift apart by two characters per comment, so a position counted in the copy the rule reads points past the character it is about, and a fix written there never reaches the file. Both are answered now, so the pseudo-classes of such a selector are spaced out like any other. Where the whitespace in front of the closing parenthesis is the line break that closes an inline comment, that end is passed over instead: a space written there would take the parenthesis into the comment, and taking the break away would do the same.
	- The `selector-pseudo-class-parentheses-space-inside` rule now reports the whitespace standing in front of a closing parenthesis where a comment closes the arguments (see [#128](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/128)). The parser kept none of it, so the `never` options passed `a:not( /**/ )` over while reporting the same space in `a:not( b /**/ )`; both are now read alike.
	- The `declaration-colon-space-after` rule now looks for the whitespace after the colon where a custom property whose value holds a comment actually keeps it (see [#109](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/109)) ([@VChet](https://github.com/VChet)):
		- the `always` options no longer report `--a: /*comment*/ !important;`, whose single space is already in place, and no longer pass over `--a:/*comment*/ !important;`, which has none at all;
		- their fix no longer adds a space of its own on every run, so two spaces or a tab after the colon now become the single space asked for;
		- the `never` option now takes the whitespace away instead of reporting a fix and leaving the declaration as it was, and no longer reports one that has no whitespace to begin with;
		- a space or a tab standing in front of the colon is no longer counted as one standing after it.
	- The `declaration-colon-space-before` and `declaration-colon-space-after` rules now work on the declaration's own colon instead of the first colon they come across (see [#92](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/92)). A comment standing in front of the colon may hold a colon of its own, an URL for one, and used to be taken for the declaration's:
		- the fix no longer edits the text of such a comment;
		- the `never` options no longer miss the real violation hiding behind it;
		- the `always` options no longer report a declaration that is already correct.
	- The `declaration-block-semicolon-newline-after`, `declaration-block-semicolon-newline-before`, `declaration-block-semicolon-space-after` and `declaration-block-semicolon-space-before` rules now check the declarations of an inline `style` attribute (see [#49](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/49)). A page gives such an attribute a root of its own with no rule inside it, and the four rules used to pass over every declaration whose parent is not a rule, so nothing was reported for `style="color: pink;top: 0;"` in an HTML, Vue, Svelte or Astro file, while the `<style>` block of the same page was checked as usual. The top level of a Sass file is left alone as before, since a variable written there is not a declaration block.
	- The `selector-descendant-combinator-no-non-space` rule now reports a parenthesised group standing after a space where a combinator belongs, `.foo (  ) .bar` for one (see [#125](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/125)). The rule used to read a combinator only where its value was a single space, so every such selector went past unseen; a group holding one space or none, `.foo ( ) .bar` and `.foo () .bar`, is reported alongside the rest. Nothing can be written to repair such a selector, so the problem is reported and the code left exactly as it was.
	- The `selector-descendant-combinator-no-non-space` rule no longer turns itself off for a selector carrying a comment (see [#124](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/124)). One comment anywhere in a selector list used to silence the rule for the whole list, so a violation standing in a neighbouring selector went unreported along with it:
		- a descendant combinator holding comments is now measured run by run, each stretch of whitespace reported at its own position and collapsed on its own, so that every comment stays where the author put it;
		- the whitespace after a comment that follows a combinator of another kind, `.foo > /*comment*/  .bar` for one, is passed over, since there is no descendant combinator there to measure;
		- an illegal combinator carrying a comment, `.foo /*comment*/ (  ) .bar` for one, is reported as [#125](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/125) has it, and left exactly as it was.
		- a selector carrying an inline comment is read as well, which was [#158](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/158) and is answered below.
		- a selector the parser does not give back as it took it is passed over too, which is [#159](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/159): a comment standing in front of a parenthesised group is filed behind it, and nothing the rule could say about such a selector would match the file.
	- The `indentation` rule now measures the lines a comment used to hide from it (see [#194](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/194)). A selector line holding nothing but a block comment was blank in the copy the rule read and passed over in silence, and is brought to its level now, in a plain CSS file as under `postcss-scss`. A pair of literal slashes in a plain CSS selector — text to CSS, which has no comment of that kind — used to swallow the line break behind it whole, so the lines after it went unmeasured too, and are measured now. And a line that was already at its level behind a comment is no longer reported at a drifted position, so a false warning goes with the false silences.
	- The `indentation` rule now measures the lines standing behind an inline comment in a declaration's value or in an at-rule's parameters (see [#236](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/236)). The search that finds the line breaks of such a text read the break closing an inline comment as part of that comment and never handed its position over, so every line after it went unmeasured: the second line of `a { b: 1px // c` drew nothing at all, where the same file spelled with a block comment of exactly that width was reported and brought to its level. That search is handed a copy with the comments blanked out of it now, the one the rules reading a value for a comma or a bang are already handed, so a value, the arguments of a function inside it and an at-rule's parameters are all measured whichever way their comments are spelled, under `postcss-scss` and `postcss-less` alike. A line of a selector holding nothing but an inline comment is measured too, which it had not been since [#194](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/194) left such a line blank in the copy the scan reads: every line is measured in the text the file spells instead, so `a,` followed by a line carrying only `// c` is brought to the level of the list like any other. In a value and in a set of parameters such a line was measured all along, and still is. And a double slash the file spells no comment with no longer hides the lines behind it, in a selector and in a set of parameters as in a value: `a { b: url(http://x/y.png)` followed by a mis-indented line is reported like any other, `@import url(http://x/y.css)` followed by one is too, and a plain CSS selector carrying such a pair no longer has the line break behind it swallowed along with the pair. The scan is told which syntax spelled the text now, so a double slash is read as a comment only where one is spelled that way — and the `*/` closing one block comment beside the `/*` opening the next is no longer read as a pair at all, so `a { b: 1px/*x*//*y*/` followed by a mis-indented line is reported as well.
	- The `indentation` rule now reports every mis-indented line of an at-rule's parameters holding comments, at the line and column the file spells (see [#65](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/65)). The lines were sought in a copy of the parameters with the comments taken out: a line holding nothing but a comment was blank there and passed over in silence, and the warning that did appear was placed by an offset into that shorter text, at `1:30` where the file has `2:3`. The scan reads the copy the file spells now and the fix writes to it, so both mis-indented lines of the issue's `@use './button' with ( … )` are reported, each asking for the `1 tab` it needs, and every comment survives the fix that used to strip all three.
	- The `value-list-comma-*`, `media-query-list-comma-*` and `function-comma-*` rules now report a comma standing behind an address (see [#153](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/153)). The search that finds the commas of a value and of a media query list read the double slash of `url(http://x/y.png)` as the opening of an inline comment, so everything from there to the end of the line was comment text to it. In a value that took the closing parenthesis of the address with it, and the count of open parentheses never came back down, so every comma that followed — on the address's line and on every line after it — stood inside a function's arguments as far as the search was concerned, where it passes them over; a media query list is read without skipping a function's arguments, so there it was the commas on the address's own line that were lost. The four `function-comma-*` rules read the same double slash the same way, through a pattern of their own, and said nothing about the comma of `image-set(url(//cdn/a.png) 1x ,url(//cdn/b.png) 2x)`. All twelve are told which double slash opens a comment and which only looks like one now, so `a { b: url(http://x/y.png),'z'; }` is reported like any other value list, and fixed by the ten of them that fix anything. Nothing about this needs a comment anywhere, or any syntax but plain CSS: an address on its own was enough.
	- The `declaration-bang-space-*`, `media-feature-colon-space-*` and `media-feature-range-operator-space-*` rules now read what stands behind an address (see [#215](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/215)). The search these rules place their matches with reads a double slash as the opening of an inline comment wherever it stands, the one in `url(http://x)` among them, and everything from there to the end of the line was comment text to it, so `a { b: url(http://x)!important; }` drew nothing from either bang rule and the second feature of `@media (min-width: url(http://x)) and (max-width:1px)` drew nothing from any of the other four. They are told which double slash opens a comment and which only looks like one now, as [#153](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/153) told the comma rules; nothing about this needs a comment anywhere, or any syntax but plain CSS.
	- The `value-list-comma-*`, `media-query-list-comma-*`, `function-comma-*`, `declaration-bang-space-*`, `media-feature-colon-space-*` and `media-feature-range-operator-space-*` rules now read three double slashes the file does not spell a comment with (see [#216](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/216)). The search these rules place their matches with was handed a copy in which every double slash that opens no comment is written over, and three of them could not be: the `*/` closing one block comment and the `/*` opening the next spell a pair between them, and neither slash can be written over without taking a comment away, so `1px/*x*//*y*/,2px` drew nothing; a pair whose second slash opens a block comment was held back for the same reason, though the one in `url(a//*b*/)` opens nothing, so `url(a//*b*/),2px` drew nothing either; and a pair standing in plain CSS, which spells no comment that way at all, was read as one, so `myurl(//a),2px`, `image-url(//a),2px` and `a//b,2px` drew nothing either. The comments are blanked out of the copy now, delimiters and all, so no reader is left to find them for itself, and a double slash is taken for a comment only where the syntax spells one — which plain CSS does not, and `postcss-scss` and `postcss-less` do.
	- The `function-whitespace-after` rule now reads what stands behind an address (see [#225](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/225)). It places its matches with the same search the rules above do, and was left on the old reading of it, so the closing parenthesis of `a { b: myurl(//a)red; }` drew nothing. It is handed the copy the others are handed now. The blindness was never plain CSS's alone: the search reads the double slash of `url(http://x)` as opening a comment whatever the file is written in, so `@import url(http://x)screen;` drew nothing under `postcss-scss` and `postcss-less` either, and the parenthesis in front of `screen` is reported now whichever of the three the file is.
	- The `media-feature-parentheses-space-inside` rule now reads a media feature standing behind a double slash the file spells no comment with (see [#225](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/225)). It finds the comments of the parameters for itself, to keep `postcss-value-parser` out of their text, and read a double slash as opening one whatever syntax had spelled them, so everything behind `myurl(//a)` was comment text to it and neither space of the second feature of `@media (myurl(//a)) and ( min-width:1px )` was reported. That scan is told which syntax spelled the parameters now, as the searches above are. An address was never the trouble here: the scan told the double slash of `url(http://x)` from a comment all along.
	- Every `*-multi-line` option now counts a form feed as the line break it is (see [#244](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/244)). `isSingleLineString` — which `whitespaceChecker` asks for every one of those options, and which `block-closing-brace-empty-line-before`, `block-closing-brace-newline-before`, `function-parentheses-newline-inside` and `function-parentheses-space-inside` ask directly — read a line feed and a carriage return and nothing else, so a construct broken with a form feed alone passed for a single line and every multi-line option passed it over in silence: `a {color: pink;\ftop: 0;}` drew nothing from `block-opening-brace-newline-after`, `a { b: fn(1,\f2); }` nothing from `function-parentheses-newline-inside`. Thirty-five rules ask that question, and all of them read the three characters every syntax this plugin reads through ends a line on now, as [#173](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/173), [#196](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/196), [#204](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/204) and [#209](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/209) each taught one reading before them. Two rules were left half repaired by that change, each because a second reading of its own counted line feeds alone, and each already wrong that way for a bare carriage return: `block-closing-brace-empty-line-before` read an empty line as two line feeds, which is [#245](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/245), and `function-parentheses-newline-inside` asked whether the break it wants is already standing by looking for a line feed, which is [#246](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/246). Both are answered below.
	- The `no-eol-whitespace` rule now sees the whitespace at the end of a line a form feed ends (see [#247](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/247)). The scan that finds where the lines end read a line feed and a carriage return and nothing else, so the space in `a { color: pink; \f}` was reported by none of the rule's options and left standing by `--fix`, while the same stylesheet spelled with any of the other three breaks was reported at `1:17` and came back trimmed. The three characters every syntax this plugin reads through ends a line on are read now, in the text of a comment as in the CSS around it, at the end of the file, and where `ignore: ["empty-lines"]` measures a line for emptiness. The warning keeps the position postcss gives it, which counts a form feed as no line at all, so a file broken with one draws every one of its warnings on line 1 — a reading of the file that happens before any rule of this plugin and is not one it can put right.
	- The `function-whitespace-after` rule now reads a call whose name closes on a digit or an underscore where no other call is open around it (see [#230](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/230)). The search the rule used to place its matches with read a parenthesis as a call's only where an ASCII letter stood in front of it, and let every parenthesis standing inside a call's arguments past whatever named it, so `a { b: atan2(1,2)red; }`, `a { b: a_(1)red; }` and `@import col-6(a)screen;` drew nothing while the same three calls nested inside another were reported all along. The rule looks for the calls of the text now, rather than coming across the parentheses of it. The `never` half of an escaped parenthesis is read now too: `a { b: url(a\)b) red; }` drew nothing, the search having placed the address's closing parenthesis two characters early, and the space behind the address is closed up like any other.
	- The `function-whitespace-after` rule now reads a call whose name CSS spells in a way a run of ASCII characters cannot (see [#252](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/252)). The name was read back from the parenthesis as a run of letters, digits, underscores and hyphens, and an identifier is wider than that in three directions and is not a run of characters at all: it may hold code points outside ASCII, it may close on a hyphen, it may open on anything behind a leading `--`, and an escape spells one of its characters with several. So `a { b: 日本(1)red; }`, `a { b: foo-(1)red; }`, `a { b: --1(2)red; }` and `a { b: fo\6f(1)red; }` — the last of them `foo`, spelled with an escape — drew nothing under either option. Less names one call by an operator rather than by an identifier, and `a { b: %("%dpx", @a)red; }` drew nothing either. The name is read forwards now, the way the scan already steps over a backslash, so all five are read wherever they stand. The refusal that keeps a dimension from naming a call reaches across such a character now as well, so `a { b: 2\65 f(1)red; }` and `a { b: 2日e(1)red; }` stop being read: an escape and a code point outside ASCII are characters of a name, and a name opening on a digit is none.
	- The `media-feature-colon-space-*`, `media-feature-range-operator-space-*` and `media-query-list-comma-*` rules now leave the arguments of such a call alone (see [#252](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/252)). These eight read a media query out of every parenthesis no call opened, so a call whose name they could not read handed them its arguments: `@media a-(max-width:600px)` and `@media fo\6f(max-width:600px)` were reported as a feature, and a fix written there landed inside the call. It runs the other way too, where the wider reading refuses a run the narrow one accepted: `@media 2日e(max-width:600px)` and `@media 2\65 f(max-width:600px)` hold a feature rather than a call's arguments now, an escape and a code point outside ASCII being characters of a name and a name opening on a digit being none. The parentheses of a call are passed over, whatever the file names it — save for `and`, `or`, `not` and `only`, which join two media features rather than open a call whatever the case they are written in, and which these rules go on reading as they always have.
	- The `media-feature-colon-space-*`, `media-feature-range-operator-space-*` and `media-query-list-comma-*` rules now read a media query whose parenthesis stands behind something that names no call (see [#230](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/230)). Nothing is named in front of the parentheses of `@media -(min-width:1px)`, `@media 2-(width>=1px)`, `@media 2(a,b)` or `@media 2and(a,b)`, a number and an operator being all that stands there; all four used to be read as a call's arguments, which these eight rules pass over, and every colon, range operator and comma inside them went unreported.
	- The `block-closing-brace-empty-line-before` rule now counts an empty line however the file spells its breaks (see [#245](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/245)). An empty line was read as two line feeds with nothing but horizontal whitespace between them, so a block broken with bare carriage returns or with form feeds had none to the rule whatever it held: under `never` `a {\rcolor: pink;\r\r}` and `a {\fcolor: pink;\f\f}` drew nothing where their line-feed twin was reported and trimmed. Each of the four spellings is read as the break it is now, a Windows pair counting as one, so a lone `\r\n` still leaves no empty line behind it.
	- The `media-feature-range-operator-space-*` rules now read a range operator standing right behind another one (see [#240](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/240)). The search that places their matches reports every two-character operator twice — once whole, and once again by the `=` its second character matches on the next index — and the guard that dropped the second of the two asked whether a `<` or a `>` stood in front of the match rather than where the operator before it ended. So every match standing behind one of those two characters was passed over, whatever it was: the whole of the `>=` in `@media (a<>=b)` and the second `<` of `@media (a<<b)` alike, each read as the leftovers of its neighbour rather than as an operator of its own. In `media-feature-range-operator-space-after` that silence lasted until a run of `--fix` had put a space between the two, so a row of such operators took as many runs as the row is long, each run reporting a problem the one before it had left; in `media-feature-range-operator-space-before` it never ended, since the space that rule writes goes in front of the row rather than between its operators. A row of `>=` was read in full all along, every member of it ending in `=`; one run of either rule now reports every operator of any row and writes every space its option asks for.
	- The `declaration-block-trailing-semicolon` rule now reads the declaration a comment closes the block behind (see [#217](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/217)). Such a comment is a node of its own, and the last one of the block, so the declaration in front of it was never looked at: `a { color: pink /* c */ }` drew nothing under `always` where `a { color: pink }` drew `Expected a trailing semicolon`, `never` passed `a { color: pink; /* c */ }` over the same way, and `--fix` left both files as they were. The block's `raws.semicolon`, which the rule reads to tell whether the semicolon is there, speaks of the last node that is not a comment, and the declaration is now sought the same way, so the two agree by construction. A nested rule or at-rule closing the block is not passed over: the flag speaks of it then, and the declaration in front of it keeps its own semicolon. Under `postcss-scss` this reaches a declaration an inline comment closes the block behind as well, and the semicolon is written in front of that comment rather than into it. Where `never` finds the semicolon behind a bodiless at-rule or behind a custom property with a comment closing the block, the problem is reported rather than fixed: PostCSS writes that semicolon back whatever the flag is set to, since without it the comment would be read as more of the at-rule's parameters or of the custom property's value.
- A warning now asks for what its option asks for:
	- The `value-list-comma-space-before` rule no longer says `Unexpected whitespace` where `always-single-line` wants a space put in (see [#175](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/175)). The message of that option was spelled with the text of the `never` ones, telling the reader to take away the very space the fix then writes, and every sibling rule already words it the other way around. It reads `Expected single space before "," in a single-line list` now.
	- The `indentation` rule now asks for an indentation a file can be written with where a closing parenthesis opens a line (see [#237](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/237)). Two subtractions of one reading took the count a step past the outermost level of the text being measured, and at the root that step fell out of what a file can hold: `@media (min-width: 100px` with a tab and a closing parenthesis on the line below drew `Expected indentation of -1 tabs`, which is no level at all. Both are answered:
		- a closing bracket opening a line unwinds only a bracket that was opened at the end of a line, one opened in the middle of a line having raised nothing. Such a closing bracket therefore asks for the outermost level of the selector, the value or the set of parameters it belongs to — the first column for an at-rule at the root, the level of the at-rule itself for one nested in a rule, the level of the declaration for a value. A selector reads the same way, `a:not(b` with a closing parenthesis alone below it among them, and so does the brace of a Sass interpolation, `@media (min-width: #{$a` with a closing brace below it;
		- the level a text is measured at is read off the caller rather than guessed from the node. A value and a set of parameters stand one level above their node, so the parentheses their first line opens are already counted and one level comes off again; a text standing at the node's own level counts nothing and owes nothing, and four do — `@nest` and Sass's `@at-root`, whose parameters are a selector, and whatever `except: ["param"]` or `except: ["value"]` takes a level away from. The discount fired for those four all the same, so every line inside their brackets was measured one level below the node and a closing parenthesis one level below that: `@at-root (without: media` with a closing parenthesis alone below it drew `-1 tabs` under the plain `"tab"` option and no secondary options at all.

		What `--fix` writes moves with the count. It never wrote a negative indentation — the count was held at nothing where the indentation is written and not where it was reported — so under a primary option alone a text measured at the root of a stylesheet comes out of the fixer exactly as it did, the message having named a level the fixer never wrote. Everywhere else the fixer used to write the first column where the outermost level stands above it, and writes the level now: the closing parenthesis of a declaration's value moves under the declaration, the one closing a nested at-rule's parameters under the at-rule, and under either `except` option the lines inside a bracket are indented by that bracket, the option taking a level from the text and not from what stands inside it.
- False negatives under `--fix` are fixed — a warning that used to vanish along with a fix that never reached the file now survives the run:
	- A fix written to a declaration's value or to an at-rule's parameters now reaches the file under `postcss-scss`, even where that value or those parameters hold an inline comment (see [#115](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/115)). That syntax keeps two copies of such a text and prints the one no rule was writing to, so `--fix` used to report a clean pass on a file it had not touched, and the warning went with it. Every rule that fixes a value or a set of parameters is affected:
		- the fix now arrives, so a stylesheet carrying a `//` comment converges instead of drawing the same warning on every run;
		- a position reported inside a value or a set of parameters no longer drifts two characters per comment, so it points at the character the warning is about;
		- the `media-feature-parentheses-space-inside` rule no longer rewrites every `//` comment of a media query into a block comment as a side effect of its own fix;
		- an inline comment closed by a bare carriage return, or by a form feed, no longer hides the rest of the declaration or of the media query from the rules that look through it for a bang, a comma, a colon or a range operator.
	- The `selector-attribute-brackets-space-inside` rule now writes its fix to both spellings of a selector carrying an inline comment under `postcss-scss` (see [#190](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/190)). The syntax keeps two — the raw the rule reads, with every `//` comment spelled as a block one, and the copy the file spells, which is the one that is printed — and the rule wrote only the first, so on such a selector `--fix` reported a clean pass on a file it never changed. The copies it left out of step also grew into corruption beside a rule that reads the pair: `.a // c` came back doubled, `.a // ca // c`, wider with every further run. The positions of its warnings drifted two characters per comment for the same reading, and stand in the file's own coordinates now.
	- Nine more rules now write their fix to both spellings of a selector carrying an inline comment under `postcss-scss` (see [#193](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/193)): `selector-pseudo-class-case`, `selector-list-comma-space-before` and `-after`, `selector-list-comma-newline-before` and `-after`, `selector-max-empty-lines`, `selector-attribute-operator-space-before` and `-after`, and `no-eol-whitespace`. Each wrote only the copy it reads, never the one the file spells and the stringifier prints, so on such a selector `--fix` reported a clean pass on a file it never changed — and left the two copies out of step for whatever read the pair next, the corruption of [#190](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/190). The positions of their warnings drifted two characters per comment for the same reading, and stand in the file's own coordinates now. In `no-eol-whitespace` the whitespace at stake may lie in the text of the comment itself, so that rule fixes the copy the file spells and refills the raw beside it, the way a declaration's value is written. And a comma whose whitespace holds the break that closes an inline comment is reported and left standing by the four `selector-list-comma-*` rules, since taking that break away, or writing a space over it, would carry the comma — or the rest of the list — into the comment's text.
	- A Less at-variable now keeps the fix written to it (see [#99](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/99)). `postcss-less` gives such a variable two copies of its text and prints the one no rule writes, so `--fix` used to leave the file exactly as it was and drop the warning along with it, reporting a clean pass on a file it never touched. This held for every rule that fixes an at-rule's parameters: `indentation`, `no-eol-whitespace`, `number-leading-zero`, `number-no-trailing-zeros` and `string-quotes`.
	- The `indentation` rule now corrects every mis-indented line of an at-rule, and not only one of them (see [#103](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/103) and [#64](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/64)). This holds for the parameters and for anything standing between the name and them, a multi-line comment for one. The remaining lines used to keep their indentation while all of the warnings disappeared, so a second run reported nothing left to fix.
	- The `unit-case` rule now fixes an at-rule whose name is not spelled in lower case, `@MEDIA` for one (see [#104](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/104)). Such a query used to be reported and then left exactly as it was, the warning disappearing with the fix, so the problem survived any number of runs.
	- The `selector-pseudo-class-parentheses-space-inside` rule now fixes the whitespace next to a closing parenthesis that a comment stands in front of (see [#123](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/123)). The parser keeps such a comment, and the space on either side of it, in the raws of the node before it, and prints the raw one; the rule wrote to the clean value instead, so the problem was reported, `--fix` claimed a clean pass and the parenthesis stayed exactly where it was. A nested pseudo-class was left half fixed at every level. Under `always` a line break in front of the closing parenthesis now collapses to a single space where a comment stands before it, as it already did where none does.
	- The `value-list-comma-space-after`, `value-list-comma-space-before` and `value-list-comma-newline-after` rules no longer drop the warning about a comma they cannot fix (see [#126](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/126)). A comma the value does not hold is out of their reach, and they said so by returning `false` from the fixer — a refusal Stylelint has never read, since it counts a fixer as applied whatever it does. So `--fix` ended with a clean pass on a file it had not finished fixing, and only a second run over the output brought the surviving problem back. A warning of that kind now stands where it used to vanish. A comma opening the value was among them at the time, and is fixed by all three rules now, which was [#134](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/134) for two of them and [#166](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/166) for the third.
	- The `no-eol-whitespace` rule now trims the end of a Less file whose last line is an inline comment closed by a bare carriage return (see [#247](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/247)). `postcss-less` ends such a comment on a line feed and on nothing else, so where the file ends on a carriage return that break stays in the raw behind the comment's text, with the whitespace in front of it; only the very end of that raw was trimmed, and the break stands there, so `// c  \r` and `a { b: 1; } // c  \r` were reported while `--fix` came back with a clean pass on a file it had not touched. The lines of that raw are walked now, and the same file ending on a form feed — which said nothing at all until the widened scan above reached it — is trimmed alongside. A Less file ending on a line feed was never affected: that break closes the comment, and the whitespace in front of it is the stylesheet's own to trim.
	- The `no-eol-whitespace` rule now trims an inline comment that holds nothing but whitespace (see [#254](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/254)). Such a comment is filed as an empty text with every character of the whitespace in the raw in front of it and nothing in the raw behind, and only the raw behind was trimmed at its end, so a line holding `//  ` and nothing else was reported while `--fix` came back with a clean pass on a file it had not touched, and the warning went with it. The end of an inline comment is taken to be the end of whichever raw closes it now, so such a line comes back as `//` and a second run has nothing to say. Under `postcss-scss` that holds wherever the comment stands and whichever of the four spellings of the break closes it, the end of the file included. Under `postcss-less`, which files that whitespace elsewhere wherever a line feed closes the comment, it holds for a file that ends inside one: whatever spaces and tabs such a comment ends on are trimmed now, however many bare carriage returns or form feeds stand in front of them, while the whitespace in front of each of those breaks was trimmed already — in front of a bare carriage return by the walk over the lines of that same raw, and in front of a form feed since [#247](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/247) taught the scan to read one as the end of a line. A comment that holds text keeps the single space between the slashes and that text, which ends no line, and a block comment holding nothing but whitespace, `/*  */`, is untouched as before, its whitespace standing in front of the closing delimiter rather than at the end of a line.
	- The `linebreaks` rule now rewrites a line break standing in a set of at-rule parameters (see [#270](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/270)). The warning came from a scan of the file itself, which sees every break whichever text of whichever node it stands in, while the fix walked the tree and wrote to a selector, a value, the text of a comment and the whitespace around a node — a set of parameters being none of those, and the one at-rule the fix did reach being a Less at-variable, which `postcss-less` gives a value beside its params. So `@media (min-width: 1px),` broken with a line feed under `windows` was reported at `1:25`, `--fix` reported a clean pass, the file came back byte for byte as it went in, and a second run reported `1:25` again. The parameters of every at-rule are read and written now, in the copy the file spells, so a comment they hold survives the fix.
	- The `function-parentheses-newline-inside` rule no longer reports a problem solved that it could only half write (see [#285](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/285)). The walk that measures the whitespace behind an opening parenthesis steps over the nodes an inline comment is spelled with, and the `never-multi-line` fix stops at the first of them, so what the fix could not reach stayed and the option stayed violated: `a { t: translate( // c` followed by a line break, ` /*x*/`, a line break and ` , 1px); }` came out of `--fix` with the space behind the parenthesis taken and the line break left, and `stylelint` reported on the very file `stylelint --fix` had just called clean. The fix is written now only where it empties every stretch that was measured, and the problem is reported where it is not.
	- The `declaration-block-trailing-semicolon` rule no longer reports a Sass nested property solved and leaves it exactly as it was (see [#335](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/335)). Such a property is a declaration carrying a block, which `postcss-scss` alone reads as one — every other syntax reads `font: 12px { … }` as a rule — and the fix of this rule writes no character itself: it sets the block's `raws.semicolon` and leaves the printing to the stringifier, which drops the semicolon it is handed for a declaration carrying a block exactly as PostCSS drops it for an at-rule carrying one. So `a { font: 12px { family: serif; } }` under `always` was reported, called fixed and written back byte for byte, the warning gone with the run. The at-rule walk of the rule turns such a node away already; the fix is now declined for the declaration too, so the warning survives `--fix` rather than vanishing with a write that never lands. Whether such a semicolon can be written at all is a question of its own, and where such a property already carries one behind its closing brace the warning is raised over a file that needs nothing — that semicolon is parsed into the block's own trailing raw rather than into the flag the rule reads, which is [#336](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/336) and older than either.

### Added

- The `max-line-length` rule now has an additional `tabSize` option (see [#10](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/10)). A tab used to count as one character, so a file indented with tabs measured shorter than the editor showed it; with the option a tab reaches the next tab stop of the given width, as the CSS `tab-size` property has it, and the limit is the one the editor's ruler shows.
- The plugin now says so when it is listed in `extends` instead of `plugins` (see [#14](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/14)). It is a plugin rather than a shareable config, and Stylelint could not tell the one use from the other: it loaded the package as a config, found no rules in it, and reported every `@stylistic/` rule of the config as unknown — an error about the rules, whose cause is the field they were never reached from. A run now stops with a configuration error naming the field to move the package to.

### Fixed

- A fix that could only be written by commenting the code out is now reported instead — an inline comment ends only with a line break, so the character an option asks for has nowhere to go:
	- The `function-parentheses-newline-inside` rule no longer takes the code behind a function's opening parenthesis into an inline comment standing in front of it (see [#129](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/129)). The line break in that whitespace is what closes such a comment, so the `never-multi-line` option cannot be satisfied without commenting out the rest of the value, and the problem is now reported rather than fixed.
	- The `media-feature-parentheses-space-inside` rule no longer takes the closing parenthesis of a media feature into an inline comment standing in front of it (see [#152](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/152)). The line break in that whitespace is what closes such a comment, so the `never` option cannot be satisfied without commenting out the rest of the query, and the problem is now reported rather than fixed.
	- The `media-query-list-comma-space-before` rule no longer takes a comma standing behind an inline comment onto that comment's line (see [#137](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/137)). The line break in front of the comma is what closes such a comment, so neither option can be satisfied without commenting out the rest of the query and the block behind it, and the problem is now reported rather than fixed.
	- The `value-list-comma-space-before` rule no longer takes a comma standing behind an inline comment onto that comment's line (see [#136](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/136)). The line break in front of the comma is what closes such a comment, so neither option can be satisfied without commenting out the rest of the declaration, and the problem is now reported rather than fixed.
	- The `function-comma-space-before`, `function-comma-space-after`, `function-comma-newline-before` and `function-comma-newline-after` rules no longer write into an inline comment standing in a function's arguments (see [#135](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/135)):
		- a comma standing behind such a comment is no longer taken onto its line, since the line break in front of the comma is what closes the comment, so the problem is now reported rather than fixed;
		- a comma standing inside the text of such a comment is no longer taken for a comma of the value at all, so nothing is reported for it and nothing written near it.
	- The `declaration-colon-space-before` rule no longer rewrites a declaration whose property is followed by an inline comment into code that does not parse (see [#88](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/88)). Such a comment ends only with a line break, so neither option can be satisfied without taking the colon into the comment, and the problem is now reported rather than fixed.
	- The `block-opening-brace-newline-before` rule no longer takes the opening brace into an inline comment standing in front of it (see [#89](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/89)). Such a comment ends only with a line break, so the `never-*` options cannot be satisfied without commenting the whole block out, and the problem is now reported rather than fixed.
	- The `declaration-bang-space-before` rule no longer takes `!important` into an inline comment standing in front of it (see [#116](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/116)). Such a comment ends only with a line break, so neither option can be satisfied without commenting out the flag and the semicolon behind it, and the problem is now reported rather than fixed. A double slash belonging to an address, or standing inside a string, opens no comment of that kind, so a flag written on the line under a value that ends in `url(http://example.com/a.png)` is still pulled up to it.
	- The `declaration-block-semicolon-space-before` rule no longer takes the semicolon into an inline comment standing in front of it (see [#117](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/117)). Such a comment ends only with a line break, so neither option can be satisfied without commenting the semicolon out, and `!important` along with it, and the problem is now reported rather than fixed. The `always` options no longer rewrite everything standing between the value and `!important` either, so a comment there, the line the flag is written on, and the spelling the flag itself was given, `!IMPORTANT` included, all survive the fix. An `!important` that `postcss-less` reads out of the text of such a comment, where Less reads comment text and nothing else, no longer hides the comment from the rule either (see [#211](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/211)): `color: red // c !important` comes back from that parser as a value of `red // c` with the flag set, and the value and the raw of the flag are asked together now, since the write follows both. That holds where the semicolon already stands on the comment's line as well, `color: red // c !important;`, which the parser keeps no raw of at all.
	- The `declaration-block-semicolon-newline-before` rule no longer takes the semicolon into an inline comment standing in front of it (see [#207](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/207)). The `never-multi-line` option trimmed every trailing whitespace of the value, or of `!important` where the declaration carries it, and the line break closing such a comment went with the rest, so `color: red // keep me` followed by `;` on the next line came out of `--fix` as `color: red // keep me;`, the semicolon commented out and the declaration ending nowhere, with nothing reported. An inline comment ends only with a line break, so the option cannot be satisfied without commenting the semicolon out, and the problem is now reported rather than fixed; a block comment, an address and a string holding a double slash are still fixed; an `!important` that `postcss-less` reads out of the text of such a comment, where Less reads comment text, does not hide the comment from the rule; and the `always` options never were in that danger, since the break they write is what closes such a comment anyway.
	- The `declaration-block-trailing-semicolon` rule no longer writes the semicolon into an inline comment (see [#232](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/232)). The guard it carried asked about one raw of one node — the one standing where a bodiless at-rule's closing brace would be — so it held under `postcss-scss`, which keeps such a comment in that raw, and nowhere else. `postcss-less` keeps a comment behind a value inside the value and one behind an at-rule's parameters inside the parameters, so `color: pink // keep me` closing a block came out of `--fix` as `color: pink // keep me;`, the semicolon commented out and the block ending nowhere, with nothing reported, and `@include x // keep me` went the same way. Whichever text the node the semicolon is written behind ends with is asked now, the value and the raw of `!important` together where that node is a declaration, so an `!important` that `postcss-less` reads out of the text of such a comment does not hide it either; an inline comment ends only with a line break, so the `always` option cannot be satisfied without commenting the semicolon out, and the problem is now reported rather than fixed. An address and a string holding a double slash are still fixed, plain CSS spells no comment with a double slash at all, and the `never` option never was in that danger, since it takes a semicolon away rather than writing one. A block comment standing behind a bodiless at-rule is still fixed as well, and so is one standing behind the value of a custom property, which every syntax keeps inside that value; behind the value of an ordinary declaration such a comment never reached this rule at all, since it is read as a node of its own and the declaration is then not the last of its block. A custom property written on a single line is held back for the same reason as everything else: nothing closes the comment in front of the brace, so the semicolon would stand inside it. One broken across two lines is not: both syntaxes read such a value to the end of the line and take the line break into it, so the semicolon lands past the comment rather than inside it, which is [#287](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/287).
	- The `function-parentheses-newline-inside` rule no longer takes a function's closing parenthesis into an inline comment standing in front of it (see [#113](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/113)). Such a comment ends only with a line break, so `never-multi-line` cannot be satisfied without commenting out the parenthesis, and everything the declaration has left, and the problem is now reported rather than fixed. The `always` and `always-multi-line` options add a line break rather than take one away, and are unaffected.
	- The `function-parentheses-space-inside` rule no longer takes a function's closing parenthesis into an inline comment standing in front of it (see [#114](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/114)). Such a comment ends only with a line break, so no option can be satisfied without commenting out the parenthesis, and everything the declaration has left, and the problem is now reported rather than fixed. All four options are asked, the guard belonging to the fix rather than to any one of them. A value with no comment in it is fixed exactly as before.
	- The `function-parentheses-space-inside` rule no longer takes the code behind a function's opening parenthesis into an inline comment standing in front of it (see [#280](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/280)). The line break in that whitespace is what closes such a comment, so no option can be satisfied without commenting out the first argument, and everything the declaration has behind it, and the problem is now reported rather than fixed — which is what the closing parenthesis has done since [#114](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/114), and what the sibling rule's opening parenthesis has done since [#129](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/129). All four options are asked, and each of them under both readings of a line break, the way [#132](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/132) leaves the closing guard asking: a fix neither language is at risk from goes through as before, and so does every fix of a value with no comment in it.
	- The `block-opening-brace-newline-after` and `declaration-block-semicolon-newline-after` rules no longer take the code behind an inline comment into that comment (see [#248](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/248)). The line break their `never-multi-line` option takes away is the one that closes such a comment, so whatever stood behind it — a declaration, a nested rule, a whole at-rule and its block — went into the comment's text, and out of the stylesheet altogether: what the fixer wrote parsed perfectly well and the next run had nothing to say about it, though the block had lost everything it opened with and nothing anywhere said so. The problem is now reported rather than fixed. The whole run of comments a block opens with is asked about rather than the last of them alone, since that option takes every break in front of the first node away at once, and a block comment standing between an inline one and that node used to be carried into the inline comment along with it. Where the comment is one `postcss-less` reads an `!important` out of the text of, the flag is asked about with it as [#211](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/211) had it; and where a value ends in the break that closes its own comment, the semicolon standing behind that break is asked about too, so a fix that would comment nothing out is written as before. The `always` options were never in this danger and are untouched, since the break they keep or write is what closes such a comment anyway. In `declaration-block-semicolon-newline-after` a comment a form feed closes is held back with the rest, which is [#295](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/295): Sass ends a comment on that character and Less does not, and the shared guard reads only the breaks the two of them agree on.
	- The `block-closing-brace-space-before` and `block-closing-brace-newline-before` rules no longer take a block's closing brace into an inline comment standing in front of it (see [#231](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/231)). The whitespace between the last node of a block and its brace is what these fixes write over, and the line break in it is what closes such a comment, so every option of the first rule and the `never-multi-line` option of the second commented the brace out along with everything the block had left: `a {` followed by `color: pink // c` on a line of its own came back from `--fix` as a file with no closing brace at all, which neither Sass nor Less reads, however happily `postcss-less` parses its own output. The problem is now reported rather than fixed. A break the fix leaves standing is the one exception. Neither rule reaches the whitespace the last node of the block ends with, so a break standing there closes the comment whichever of the two is asked; and the first writes over only the whitespace ending the block's own final raw, so a break in front of a stray semicolon of that raw survives it as well, where the `never-multi-line` fix of the second takes every whitespace of the raw away. Wherever such a break stands, the brace lands outside the comment and the fix runs exactly as before. The `always` options of the second rule were never in this danger, since the break they write is what closes such a comment anyway, and a block whose brace a comment had already swallowed is put right by them.
- A fix no longer carries off what stands next to the character it changes:
	- The `function-comma-*` rules no longer rewrite a comment standing elsewhere in the value they fix (see [#238](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/238)). All four wrote their fix back as the whole value printed anew by `postcss-value-parser`, and that printing does not always give back the text it was handed: a comment opening `/*/` comes back as `/**/`, so `a { b: x/*/*a,b*/f(1,2)c; }` came out of `function-comma-space-before` under `always` as `a { b: x/**/*a,b*/f(1 ,2)c; }` — what was one comment now a comment and three characters of code, and the value naming something else. The whitespace an option asks for is written at the position of the comma it belongs to now, so every other character of the value is left exactly as it was, whatever the parser would have made of printing it.
	- The `function-parentheses-space-inside`, `function-parentheses-newline-inside`, `media-feature-parentheses-space-inside`, `color-hex-case` and `unit-case` rules no longer rewrite a comment standing elsewhere in the value they fix (see [#272](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/272)). All five wrote their fix back as the whole value, or the whole set of media parameters, printed anew by `postcss-value-parser`, and that printing does not always give back the text it was handed: a comment opening `/*/` comes back as `/**/`, so `a { b: x/*/*a*/#aabbcc; }` came out of `color-hex-case` under `upper` as `a { b: x/**/*a*/#AABBCC; }` — what was one comment now a comment and three characters of code, and the value naming something else. Each fix is written at the position of the whitespace, the colour or the unit it belongs to now, so every other character is left exactly as it was, whatever the parser would have made of printing it. Two things travel with that. A call holding nothing but comments now gets the line break each half of `function-parentheses-newline-inside` under `always` asks for rather than one break for the two, since the closing half no longer reads the value as the opening fix has already left it: `a { b: f( /*c*/ /*d*/ ); }` reported two problems and wrote one of them. And a media query whose fix Stylelint never called — a `stylelint-disable` comment over the at-rule is one way — is no longer printed anew for nothing.
	- The `declaration-bang-space-*` rules no longer write into the characters standing between two bangs of one declaration (see [#239](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/239)). Both rules share a checker that finds the bangs of a declaration in the text as the file spells it and hands each of them to a fix that writes at the index it was given, and that index was counted before any earlier write had changed the length of what it replaced, so the second write landed as far past its mark as the first had lengthened the text in front of it. `a { b: 1px!important 2px /*c*/!important; }` came out of `declaration-bang-space-before` under `always` as `a { b: 1px !important 2px /*c* /!important; }`, the delimiter closing the comment cut in two, and the next run reported `Unclosed comment` on a file that no longer parses. `a { b: 1px!important 2px!important; }` came out as `a { b: 1px !important 2p x!important; }`, a space written into the middle of the value, and `a { b: 1px!important//!important; }`, whose double slash plain CSS spells no comment with, as `a { b: 1px !important/ /!important; }`. `declaration-bang-space-after` reached the same three shapes from the other side, writing the space in front of the second bang rather than behind it: `a { b: 1px!important 2px!important; }` came out as `a { b: 1px! important 2px !important; }`, whitespace at a position belonging to no bang of its own, and a further run was wanted to put a space where the option had asked for one. Every bang is given the whitespace its option asks for in one run now, each write made where its own bang stands, so every other character of the declaration is left exactly as it was and a second run finds nothing left to do.
	- The `function-whitespace-after` rule under `never` no longer closes up the whitespace that makes a sign the operator of a sum (see [#257](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/257)). A `+` or a `-` is part of the number behind it only where a digit or a decimal point follows it; anywhere else the sign stands alone, and the whitespace in front of it is what a calculation is read by. So `a { b: calc(var(--x) + 1px); }` came out of the fix as `calc(var(--x)+ 1px)` — no longer a sum, and a declaration the browser drops rather than one merely respaced. That whitespace belongs to the sum rather than to the call now, and the reading asks nothing about the call it stands behind, so a preprocessor's arithmetic keeps its spaces with it — `a { b: translate(1px) + 2px; }` is left as it is written. A comment standing between the call and the operator no longer hides the sum, `calc(min(1px, 2px) /**/+ 1px)` being one as much as `calc(min(1px, 2px) + 1px)` is. A sign that does open a number is untouched, `a { b: url(x) -1px; }` closing up as before, and so are a `*` and a `/`, the grammar asking for no whitespace beside either. What the reading costs is a warning: a sign standing in front of a name rather than a number is read as an operator too, so `a { transform: translate(1px) -webkit-foo; }` is no longer reported, telling the two apart calling for a list of every name a calculation may hold. `always` is untouched as well: it asks for the whitespace the grammar wants, so `calc(var(--x)+ 1px)` is repaired by it rather than damaged.
	- The `function-whitespace-after` rule under `never` no longer closes up the whitespace that a preprocessor's own arithmetic is spelled with (see [#264](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/264)). The reading [#257](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/257) gave the rule is the one CSS has, where `-1px` is a single number: a sign with a digit or a decimal point behind it opens a number there rather than a sum, and the space in front of it belongs to the call. Sass and Less spell arithmetic of their own, in which such a sign is an operator all the same — `a { b: foo($a) -2px; }` is a list of two values and `a { b: foo($a)-2px; }` a subtraction — so the fix wrote the one in place of the other, and wrote it the same way into `a { b: foo(@a) -2px; }` under Less. The rule asks which syntax spelled the node now, the way it already asks in order to know what a double slash opens in it, and under a syntax spelling arithmetic of its own it leaves the whitespace in front of every sign where the author wrote it. The question is put to the node rather than to the file, so a page holding a plain `<style>` beside a `<style lang="scss">` has each of its blocks read by the syntax that spelled it. What the reading costs there is a warning, in two places: `a { b: url(x) -1px; }` is no longer reported under those syntaxes, telling such a list from a subtraction being the work of the language's compiler rather than of a rule about whitespace; and a plus is left alone under Sass, which reads one as an operator whatever whitespace stands beside it, since the question the rule puts to a syntax tells a preprocessor from plain CSS and does not tell Sass from Less, and one reading for the two is chosen over a second probe. A file of plain CSS is untouched, and so is `always` under every syntax; a syntax that answers nothing about itself is read as a preprocessor here, which leaves whitespace standing rather than a file broken.
	- The `selector-pseudo-class-parentheses-space-inside` rule no longer writes a descendant combinator into a selector whose comment closes a pseudo-class's argument (see [#161](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/161)). `postcss-selector-parser` keeps no whitespace between such a comment and the end of the argument it closes, and hands the run to whatever stands behind the closing parenthesis instead, so `a:not( /*c*/ ):is(b)` came back out of the parser as `a:not( /*c*/) :is(b)` — a compound selector turned into a descendant one, matching other elements than the author wrote it for. Every fix this rule made in such a selector carried that into the file, whichever pseudo-class of it the fix was made in, and no warning ever mentioned it. A comma does the same seen from the other side, `a:not(b, /*c*/ , c)` losing the space standing in front of it. The rule now gives the parsed selector back what the parser dropped before it measures anything, so a fix writes what the option asks for and nothing else.
	- The `string-quotes` rule no longer drops a comment standing in the selector when it fixes an attribute value there (see [#178](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/178)). The rule read `ruleNode.selector`, a copy PostCSS hands it with every comment taken out, and wrote the fix back to the same place, so `.foo /* x */ [title="y"]` came out of `--fix` as `.foo  [title='y']` — the comment gone and the whitespace it stood in left behind — and the warning pointed into the comment rather than at the quote it was about. The rule reads the text the file holds now and writes the fix to it, the way the other selector rules have since [#124](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/124): the comment keeps its place, the warning stands at the quote, and under `postcss-scss` an inline comment of the selector keeps the spelling the file gives it, its text never read as strings of the selector.
	- The `indentation` rule no longer carries off the comments of a selector it re-indents (see [#194](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/194)). It read and wrote `rule.selector`, a copy PostCSS hands it with every comment taken out, so its lines were measured against a text the file does not hold and its fix printed without the comments — `b // c` came back as `b ` with a trailing space, and a block comment in a plain CSS selector went the same way. The rule measures the copy the file spells now and writes the fix to it, refilling the raw beside it under `postcss-scss`, so every comment stays where the author put it. The text of each inline comment is blanked out of the scan's own copy, since `style-search` reads the line break that closes one as part of it and would pass the lines behind it over; the blanks keep every position where it was.
	- The `block-opening-brace-space-before` rule no longer removes a comment standing between the selector and the opening brace when fixing (see [#63](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/63)):
		- a block comment is kept, and only the whitespace next to the brace changes, so such a stylesheet can now be autofixed without losing anything;
		- an inline comment leaves the brace nowhere to go, since the comment ends only with a line break, so the problem is now reported rather than silently rewritten.
	- The `declaration-block-trailing-semicolon` rule no longer damages the block when it adds a missing semicolon after a bodiless at-rule (see [#87](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/87)):
		- a comment standing between the at-rule and the closing brace is kept;
		- the closing brace stays on its own line instead of being pulled up to the at-rule;
		- an inline comment leaves the semicolon nowhere to go, since the comment ends only with a line break, so the problem is now reported rather than silently rewritten.
	- The `indentation` rule no longer damages a declaration whose multi-line value holds comments when fixing (see [#62](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/62)). The mis-indented lines were found in the text that keeps the comments while the fix was written to a copy with the comments taken out, so every write landed past the line break it was aimed at — `--fix` cut the `0` out of `$somevar: ( /* some comment */ … 'a_property': 0 … )` and carried every comment off with it. Both sides work on the text the file holds now:
		- the fix re-indents every mis-indented line of the value, and the comments and the value survive it;
		- a comment closing the line that opens the parentheses no longer costs the warning a level, so the lines inside are asked for the `1 tab` they need where `0 tabs` used to be expected.
	- The `unit-case` rule no longer drops the comments a value or a set of at-rule parameters holds when it changes the case of a unit (see [#233](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/233)). It read `decl.value` and `atRule.params`, the copies PostCSS hands back with every comment taken out, and wrote the whole text back to those same bare properties, discarding the raw the comments live in — so `a { b: 1px /* c */ 2px; }` came out of `--fix` as `a { b: 1PX  2PX; }`, and nothing was reported:
		- every comment a value or a set of parameters holds survives the fix, in plain CSS and under both custom syntaxes, and under `postcss-scss` a comment written with a double slash survives it too;
		- a warning about a unit standing behind a comment now points at the column the file has it in, since the text the position is counted in is the text the file holds;
		- a unit standing inside an end-of-line comment a value holds is recased and reported under `postcss-scss` as well now, where only `postcss-less` used to reach it: the copy the file spells is the copy that carries such a comment, and leaving its text alone again is [#271](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/271).
	- The `linebreaks` rule no longer drops the comments a value holds when it rewrites the line breaks of a file (see [#235](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/235)). It rewrote every value it walked, and the copy it rewrote was the one PostCSS hands back with the comments taken out, so a value spanning lines lost them all — `a { b: 1px /* c */` and `2px; }` on the line under it came out of `--fix` under `windows` with the comment gone. The break is now written into the copy the file spells, comments and all, and the breaks a comment itself holds are rewritten with the rest, since a break standing inside a comment is a break of the file like any other.
	- The `linebreaks` rule no longer drops the comments a selector holds when it rewrites the line breaks of a file (see [#269](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/269)). This is [#235](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/235) one property over: the fix wrote `rule.selector`, the copy PostCSS hands back with every comment taken out, and PostCSS threw the raw those comments live in away along with the copy `postcss-scss` prints, so `a /* c */,` broken with a line feed over `b { c: d; }` came out of `--fix` under `windows` as `a ,` and the comment was gone, with nothing reported. A comment written with a double slash went the same way under `postcss-scss`. The selector is read and written in the copy the file spells now, so every comment survives the fix, in plain CSS and under both custom syntaxes. A selector holding no line break was never affected, and by accident: the fix hands such a selector back unchanged, so the bare copy still matched the raw and PostCSS went on printing the raw.
	- The `number-*` rules no longer drop the comments a value or a set of at-rule parameters holds when they add or take away a zero (see [#268](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/268)). These rules read `decl.value` and `atRule.params`, the copies PostCSS hands back with every comment taken out, and wrote the fix back to those same bare properties, discarding the raw the comments live in — so `a { b: 1px /* c */ 0.50em; }` came out of `--fix` under `number-leading-zero` with the option `"never"` as `a { b: 1px  .50em; }`, and nothing was reported. They read and write the copy the file spells now, so every comment keeps its place, under `postcss-scss` a comment written with a double slash keeps it too, and a warning about a zero standing behind a comment points at the column the file has it in. Under `postcss-less` that raw also carries the whitespace behind an end-of-line comment, which used to go with the comment and take the semicolon of the declaration into it. One thing goes the other way, and under `postcss-scss` alone: a fraction spelled inside an end-of-line comment such a value holds is now rewritten and reported along with the rest, where only `postcss-less` used to reach it. The copy the file spells is the copy that carries such a comment, and leaving its text alone again is [#271](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/271).
	- The `linebreaks` rule no longer closes up a line break it has just reported (see [#293](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/293)). The fix deleted every carriage return of a text and then, under `windows`, wrote a pair in front of every line feed — right for a Windows pair, whose line feed carries the break through the first pass, and wrong for a carriage return standing on its own, which has no line feed behind it for the second pass to find, so both options took such a break away altogether. `a {}` and `b {}` parted by a bare carriage return came back as `a {}b {}` under `unix`, and under `postcss-scss` a value whose end-of-line comment that break closed lost the rest of its block into the comment, `--fix` reporting a clean pass on the wreck. Every break the rule reads is respelled in one pass now, a Windows pair counting as one. A form feed is still neither of the two characters this rule reads, so a file broken with one is passed over as before.
- A warning is no longer raised over code that is already right, nor over the text of a comment taken for code:
	- The `selector-pseudo-class-parentheses-space-inside` rule no longer reports a pseudo-class it has already fixed, where the last argument holds nothing but comments (see [#128](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/128)). The whitespace in front of the closing parenthesis was not in the parsed selector at all, so the `always` options asked for a space that was already there and went on asking however many times the fix was run, reporting it at a column that fell inside the comment. `a:not(/**/)` is now fixed into `a:not( /**/ )` once, and the output is left alone from then on. A run of several spaces in front of that parenthesis is left as it is now, as one standing anywhere else inside the parentheses always was. None of this reaches a selector carrying an inline comment under `postcss-scss`, which the rule reads from a copy of itself that spells every one of those comments `/* */` and writes its fix to a copy that is never printed; the rule reads and writes both copies now, which was [#163](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/163) and is answered below.
	- The `function-parentheses-newline-inside` rule no longer reports an opening parenthesis that an inline comment already follows correctly (see [#141](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/141)). The `always` options let a comment stand between the parenthesis and the line break they ask for, and used to walk past a block comment while stopping at an inline one, so a function already spelled the way the option asks was reported and then reformatted by the fix.
	- The `media-feature-parentheses-space-inside` rule no longer reads a media feature out of an inline comment standing in a media query, nor writes inside such a comment (see [#138](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/138)). A parenthesis in the text of a comment used to open a feature as far as the value parser was concerned, and the fix then put the space the option asks for into that text.
	- The `selector-combinator-space-before` rule no longer reports a combinator that opens a selector when a comment stands in front of it (see [#66](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/66)). A selector list broken over several lines and interleaved with comments is now left alone instead of being pulled onto one line. Two more things follow for this rule and for `selector-combinator-space-after` alike, whenever the selector holds an inline comment:
	- The `selector-combinator-space-before` and `selector-combinator-space-after` rules no longer drop the warning about whitespace they cannot write. A comment standing beside a combinator is folded into the raws of that side, and a raw is what the parser prints in place of the spaces the fix writes, so the fix changed nothing while Stylelint counted it as applied and `--fix` ended with a clean pass on a file it had not touched. `.foo  /* c */  >  .bar` keeps its warning now, in a stylesheet of any syntax.
		- the reported position no longer drifts two characters per comment, so it points at the combinator itself;
		- the fix now reaches the output instead of being silently discarded, and the comment keeps its `//` spelling, whatever kind of combinator the comment stands beside: one folded into the raws of a `>`, `+` or `~` used to be rewritten into a block comment by a fix made elsewhere in the selector, and the position reported there drifted two characters per comment;
	- The `string-quotes` rule no longer reports a quotation mark standing inside a `//` comment (see [#32](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/32)). A Less value or an at-rule's parameters keep such a comment in themselves whenever it does not open the statement, and the quotes in its text used to be taken for a string of code:
		- the warning is gone, and the fix no longer rewrites the text of the comment;
		- a double slash opens a comment only where the syntax says one does: under `postcss-scss` those are the comments its parser found, and a value of plain CSS is read exactly as before;
		- a double slash belonging to an address opens no comment even there, whether the address is quoted or bare inside `url()`, and whatever parentheses its interpolation or its escapes bring along;
		- a comment ends with its line, so a string on the next one is checked as before.
	- The `selector-pseudo-class-parentheses-space-inside` rule no longer reports an end of the parentheses that holds no argument, nor throws when fixing one (see [#127](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/127)). The whole of `a:not()`, and the empty half of `a:not(,b)`, have no inside to space out, and the parser keeps no whitespace at such an end either — `a:not( )` comes back out as `a:not()`, and `a:not( ,b)` as `a:not(, b)`, the run standing in front of the comma handed to the node behind it. So an end with nothing at it is passed over, and where whitespace stood at one — or at a selector of the list that holds nothing, `a:not( b ) , , c` for one — the whole selector list is passed over with it, since nothing in the parsed selector can hold that run and a fix made anywhere in the list is written back from it. The end that does hold an argument is spaced out as before, and a pseudo-element written with an empty argument, `a::part()` for one, is read the same way as a pseudo-class.
		- Under `always` an empty argument standing at an end of the list used to draw two warnings, each at the parenthesis the other one was about, and the fix then threw, which Stylelint reported as a parse error: the rule lost every warning and every fix it had for that selector, so `a:not(), b:not(c)` came back exactly as it was. One standing between two arguments threw nothing and the warnings it drew were real, but the fix that answered them carried the whitespace of the empty argument off with it, `a:not(b, ,c)` coming back as `a:not( b,, c )`, so those go the way of the rest.
		- Under `never` a fix made anywhere in such a selector list used to be written back from that tree: `a:not( ,b )` came back as `a:not(, b)`. Nothing is reported there now and nothing written, so a violation standing beside the empty argument, the space in `a:not( b, )` or in the `b:not( c )` of `a:not( ) , b:not( c )`, goes unreported along with the rewriting.
	- The `string-quotes` rule now asks each block of a page about its own language (see [#101](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/101)). A stylesheet written in Less inside a `<style lang="less">` used to be read as plain CSS, so the quotes standing in the text of a `//` comment were reported and the fix rewrote them; a page may now hold blocks in several languages and each is read as its own.
	- A Less CSS guard is no longer read as ordinary CSS (see [#125](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/125)). `isStandardSyntaxSelector` used to recognise one only by accident — a guard on a selector carrying no colon looked like a mixin definition, and a condition naming a variable looked like a parametric mixin — so `.a:hover when (1 = 1)` answered to neither and every rule built on that util went on checking it. Such a selector is now passed over as any other Less construct is, wherever the guard stands, while a quoted attribute value holding the same text, `a[href$=" when ("]` for one, is ordinary CSS and is checked as before.
	- A Less CSS guard is recognised in two more shapes (see [#150](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/150)). `isStandardSyntaxSelector` asked for whitespace in front of the condition that Less does not ask for, so `.a:hover when(1 = 1)` went on being checked by every rule built on that util, and a fix could write a line break into the guard's own list of conditions, `when(1 = 1), (2 = 2)` for one. A quotation mark escaped outside a string no longer opens one either: the run blanked from `.x\'y:hover when ('z' = 'w')` used to swallow the guard along with it, which put the selector back in the rules' way. That reading cost a plain CSS selector as well, wherever an escape stood in front of an attribute value holding the text of a guard — `.a\"b [title=" when ("]  c` was passed over by every rule of the plugin, and is now read like any other.
	- The text of a quoted attribute value is no longer read as selector syntax (see [#151](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/151)). An attribute value may hold any text at all — a link matched by its address, a `data-*` carrying a template fragment — and `isStandardSyntaxSelector` used to read `[title=":extend(x)"]`, `[title="//"]`, `[title="<%"]`, `[title="%>"]` and `[title=".foo()bar"]` as preprocessor constructs, so several rules passed such a rule over in silence. Every check that reads the text of a selector now reads it with the quoted runs emptied, as the check for a Less guard already did, while a preprocessor construct standing beside such a value is recognised as before. One check asks whether the selector holds a colon, to tell a Less mixin definition from a selector ending in a functional pseudo-class, and a colon quoted inside an attribute value no longer answers it: `.mixin("a:b")` is read as the Less mixin definition it is.
	- Two comments standing side by side in a selector are no longer read as an inline comment (see [#164](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/164)). The closing `*/` of one and the opening `/*` of the next spell `//` between them, so `a:not(  b  )/*one*//*two*/c` looked like a preprocessor construct to `isStandardSyntaxSelector` and several rules passed it over (see [#151](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/151)). A comment is now taken out of the copy those checks read, delimiters and all, so the text a comment holds is no longer read as selector syntax either — while a real `//` comment is recognised as before, whatever its text spells: `a//*x*/b` is one, and not a comment of the other kind. What a comment leaves behind is read too, since it leaves the whitespace it stood in: `.a:hover/*x*/when (1 = 1)` and `.a:hover when/**/(1 = 1)` are the guards the Less compiler reads there, and are passed over now like any other guard. The colon a comment holds is no longer counted either, so `.mixin(/*:*/)` is read as a Less mixin definition, as `.mixin("a:b")` already was.
	- A Less guard keyword written in another case is no longer taken for a guard (see [#168](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/168)). Less reads its keywords in lower case only — the compiler prints `.a:hover WHEN (1 = 1)` as it stands, and answers `when NOT (1 = 1)` with a syntax error — while `isStandardSyntaxSelector` matched the word in any case and passed such a selector over. Nothing valid was silenced by that, since a parenthesis opening after whitespace is nothing a CSS selector has, but what the plugin says about such a file is now what the syntax says.
	- Interpolation standing in the text of a quoted run is no longer read as interpolation of the selector (see [#170](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/170)). `isStandardSyntaxSelector` asked whether the selector holds `#{…}`, `@{…}`, `${…}`, `{{…}}` or `$(…)` before it emptied the quoted runs, so `[title="#{a}"]` was taken for a preprocessor construct and several rules (see [#151](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/151)) passed the rule over, reporting nothing and fixing nothing. A pair of braces holding anything at all counts as interpolation of the template kind, so an attribute value carrying no construct whatever went with them — `[data-config='{a:1}']` for one, whose braces are the text of a value and nothing else. The question is asked of the emptied copy now, as every other question about the text already was, while interpolation standing outside a quoted value is recognised as before: `[title="a"]#{$b}` and `a[b=#{c}][d="e"]` are the preprocessor constructs they were. A comment holding the text of one, `a /* #{$b} */ c`, is read as the comment it is, as a comment spelling any other construct already was.
	- The text of a quoted attribute value is no longer read as a Less `:extend` (see [#171](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/171)). `postcss-less` marks a rule as an extend by matching the text of its selector, quotes and all, so `[title=":extend(x)"]` carried the mark though what stands inside the quotes is an attribute value and nothing else, and several rules (see [#151](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/151)) passed such a rule over in a Less stylesheet while checking it in every other. The mark is asked of the code now, of the same copy the checks for a preprocessor construct already read, with every quoted run emptied. A real extend is passed over as before, `&:extend(.inline)` and `.a:extend(.b all)` among it, and so is `[title=":extend(x)"]:extend(.b)`, which carries one of each.
	- The text of a value is no longer read as a Less `&:extend` (see [#181](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/181)). `postcss-less` marks a declaration as an extend by matching the word against its value, quotes and all, so `b: "extend(x)"`, `content: "a:extend(y)"` and even `b: myextend(x)` carried the mark though all three are plain declarations Less compiles as they stand, and the rules that read a declaration through `isStandardSyntaxDeclaration` passed them over in a Less stylesheet while checking them in every other. The declaration's shape is the answer now: a real extend is the one whose property is `&`, which no standard declaration has, and it is passed over as before — in any case and spacing, since everything else Less answers with a syntax error is nothing to fix either. The shape is read in every syntax, where the mark stood only in Less, so the same `&:extend(.b)` pasted into a plain CSS or SCSS file is passed over now too, instead of being respelled `&: extend(.b)` as though the colon of that construct were a declaration's.
	- The `indentation` rule no longer raises the whole of a selector list by a level when one selector of it is a pseudo-class broken over several lines (see [#74](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/74)). The extra level issue [#30](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/30) asked for was granted by a presence check over the entire selector, so the `.foo,` standing beside a multi-line `:where( … )` was asked for an indent it must not have, and `--fix` rewrote a correct file into a wrong one — the trigger being nothing rarer than a second selector in the list, in plain CSS. The level is now raised by the parenthetical-depth tracking, for the lines inside the pseudo's parentheses and for them alone, whichever position of the list the pseudo stands in, so the sole-selector case of #30 keeps its extra level and every other selector of the list keeps its own.
		- One more shape moves with the discount that granted the old level: a line standing inside parentheses that other code opened, `a:has(.foo,` broken before `.bar)` for one, is measured at the rule's own level now. At the top level that level is zero either way and only the words of the warning change, `0 spaces` where `-2 spaces` was asked; inside a block the old reading expected such a line one level below the rule itself — an outdent no other line is asked for — and the line is expected level with its rule now, so a file spelled to the old expectation draws a warning it did not draw.
	- The `max-line-length` rule no longer reports the line after one that holds both an `@import` string and a `url()` argument (see [#197](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/197)). The rule took one excluded substring off a line, so the second stayed behind and came off the next line instead, with the wrong sign: a line well within the limit was reported longer by the distance to the substring it never held, while the line holding both was measured with one of them still on it. Every excluded substring standing on a line now comes off that line, a `url()` argument standing inside an `@import` string only once.
	- The `declaration-block-semicolon-*` rules no longer report a declaration that has no semicolon where a comment closes the block behind it (see [#208](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/208)). These rules pass over the declaration a block ends on without a semicolon, and found it as the block's last node; a comment standing between that declaration and the closing brace is a node of its own, the last one, so `a { color: pink /* c */ }` was measured for a semicolon the file does not hold. The `always` options of the `-before` rules wrote a break, or a space, in front of the comment and another one on every run, the file growing a line or a space at a time; `-space-after` pulled the comment up to the declaration under `always` and closed it up under `never`; `-newline-after` stepped past the comments on its own and is left as it was. The declaration is now sought past the comments, as the flag PostCSS keeps for it already reads the block, and a nested rule or at-rule closing the block is not passed over the same way, since the declaration in front of it keeps its semicolon and is measured as before.
	- The `function-comma-*` rules no longer read a comment out of an address (see [#153](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/153)). The pattern that takes the comments out of a function's arguments before its commas are measured used to read the double slash of `url(//cdn/a.png)` as one, and cut everything from there to the end of the line away with it, so the position a comma was measured at pointed at whatever stood there instead — into the middle of a word, or past the end of what was left. A comma with the whitespace its option asks for already in place was reported that way, and the fix that followed wrote where nothing was wrong: `image-set(url(//a) 1x , // c` and a line break came back from `function-comma-newline-after` with the comment moved onto a line of its own.
	- The `media-query-list-comma-*`, `media-feature-colon-space-*` and `media-feature-range-operator-space-*` rules no longer read the arguments of a function as media syntax (see [#213](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/213)). A comma, a colon or a range operator standing inside `url(x/a,b.png)`, `url(http://x)` or `url(a>=b)` was a comma of the query list, a media feature's colon or a feature's operator to the search, so a file with nothing wrong in it drew a warning, and the fix then wrote whitespace into the address, which no longer names the same resource: `@media (min-width: url(x/a,b.png))` came back as `@media (min-width: url(x/a, b.png))`. The arguments of a call are passed over now, and a media feature keeps its own parentheses: those of `(min-width: 1px)` and of `screen and (color)` open no call, since nothing is named in front of them. The search cannot be asked this of itself — it reads the character in front of the first parenthesis of a text out of nothing at all and finds a letter there, so it takes the whole of `(min-width: 1px)` for a call's arguments. A word joining two features names no call however a file spells it, so the `and(min-width: 1px)` of a file written without the space its grammar asks for is read as the feature it is, and an escaped parenthesis inside an address closes the arguments of nothing.
	- The `function-comma-*` rules no longer read one comment where the file spells two, nor a comment where a string spells its delimiters (see [#214](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/214)). The comments were taken out of a function's arguments with a regular expression before the commas were measured in what was left. It carried no `g` flag, so only the first comment ever went, and every position behind the second was off by that comment's width; and where the `*/` of one comment was followed by code, its match grew until it found a `*/` that was not, swallowing everything in between — a comma of the arguments among it. `translate(1px/*k*/, /*c*/ 2px)` was reported for a space that is there, and `translate("a/*b", "c*/ d",1px)`, where a string spells those delimiters, was read the same way: under `always` the comma that has its space drew a warning beside the one that has none, and under `never` neither of them drew anything, the position measured for the first having landed inside a word. The comments are found by reading the arguments now, in the same pass that places the commas, so `translate("a/*b", "c*/ d",1px)` lures nothing across a comma either — the comma that has its space is left alone and the one that has none is reported, and a comment code follows straight away is left where it stands, since taking it out would put the whitespace in front of it against that code.
	- Every `*-single-line` option now counts a form feed as the line break it is (see [#244](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/244)). The same reading told those options that a construct broken with one stood on a single line, so `a\f,b {}` drew `Expected single space after "," in a single-line list` although the list holds a break, and `a { color: pink;top: 0\f}` drew `Expected single space after ";" in a single-line declaration block` from `declaration-block-semicolon-space-after`. In `function-parentheses-space-inside` the fix then wrote over the break itself: `a { b: fn(\f1px\f); }` came out of `--fix` under `always-single-line` as `a { b: fn( 1px ); }`, both form feeds gone and three lines of the file made one. Such a construct is multi-line now, so the single-line options pass it over and the multi-line ones take it up.
	- The `no-eol-whitespace` rule under `ignore: ["empty-lines"]` no longer reports the whitespace standing alone on a line that a bare carriage return or a form feed opens (see [#247](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/247)). The lookback that measures such a line for emptiness reached back to the previous line feed and no further, so `a {}\r  \rb {}` drew a warning where `a {}\n  \nb {}` and `a {}\r\n  \r\nb {}` drew none. All four spellings are read alike now, and a line holding anything but whitespace is reported as before.
	- The `function-parentheses-newline-inside` rule no longer asks for a line break the function already holds (see [#246](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/246)). Whether that break was already standing was read as `includes("\n")`, while whether the function stands on more than one line is read through the three characters that end a line, so a function broken with bare carriage returns or with form feeds was multi-line to the one reading and unbroken to the other: under `always` and under `always-multi-line` alike, `a { b: fn(\r1px\r); }` was reported for the breaks it holds and `--fix` wrote a line feed in front of each of them, `a { b: fn(\n\r1px\n\r); }`, which the next run then passed over in silence. Both options read the three characters now, and a function broken on one side only draws one warning where it used to draw two: `a { b: fn(\r1px); }` is asked for the break in front of its closing parenthesis alone, and comes back `a { b: fn(\r1px\n); }` rather than broken on both sides. The break the option asks for may also stand behind an inline comment, `a { t: translate( // c\r  1px, 2px\r); }` for one, and such a value was reported and then reformatted by the fix, which wrote a break of its own in front of the comment — the shape [#141](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/141) answered where the comment is closed by a line feed. It is accepted now wherever the syntax ends the comment on the break the file spells: a carriage return under `postcss-less` and `postcss-scss` alike, and a form feed under `postcss-scss`, which ends an inline comment on one where Less does not.
	- The `function-whitespace-after` rule no longer reads a parenthesis that groups an expression as a call's (see [#230](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/230)). The parenthesis was handed to a check that looked at the character standing behind it and at nothing else, so a Less or SCSS calculation carrying a unit was a call wherever the search reached it — where the value opened on the parenthesis, and anywhere inside a call's arguments: `always` reported `h1 { width: (@a * 2)px; }` and wrote the whitespace in, which Less then compiles as two values rather than the one the author meant, and `never` closed the two values of `h1 { width: (@a * 2) px; }` up into that same expression — so whichever of the two options a project picked, one of the shapes was rewritten into the other. The rule now looks for the parenthesis a call closes instead of coming across every parenthesis there is, so a group is passed over however deeply it is nested, whether it stands inside a call or holds one, and a negated or subtracted one, `-(@a * 2)px` and `2-(1)px`, goes with it. The options of a Less import are left alone as well: `@import (reference)"foo.less";` used to draw a warning under `always` for the quotation mark behind the parenthesis, and `@import (reference) "foo.less";` used to lose that space under `never`. Plain CSS spells a group of its own inside a calculation, and it was rewritten the same way: under `never` the fix closed `a { width: calc((100% - 20px) - 1rem); }` up into `calc((100% - 20px)- 1rem)`, and under `always` it pushed `calc((100% - 20px)-1rem)` apart into `calc((100% - 20px) -1rem)` — each of them a sum the CSS grammar no longer reads, so the declaration was dropped by the browser rather than merely respaced. A second plain CSS shape is read better than before as well: an escaped parenthesis inside an address closes no call, so `a { b: url(a\)b)red; }` is reported at the parenthesis that does close the call and the fix no longer writes a space into the address, where `always` used to put it. One more parenthesis stops being read and rightly so: a run of characters opening on a digit names nothing, so `a { b: 2px(1)red; }` and `a { b: 2and(1)red; }` are a dimension in front of a parenthesis rather than a call, and the whitespace behind them is none of this rule's business.
	- The `block-closing-brace-empty-line-before` rule under `always-multi-line` no longer asks for an empty line a block already holds (see [#245](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/245)). An empty line was read as two line feeds, so `a {color: pink;\r\r}` and `a {color: pink;\f\f}` were reported although each holds one, where their line-feed twin was passed over.
	- The `function-parentheses-space-inside` and `function-parentheses-newline-inside` rules no longer report a function whose closing parenthesis a comment has swallowed, nor write outside one (see [#131](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/131)). `postcss-value-parser` knows nothing of a comment opened by a double slash, so a `/*` standing in the text of one opens a block comment to it that never closes and takes the parenthesis with it. The function comes back marked unclosed, and the stringifier prints what such a node keeps in front of its parenthesis behind it instead, so the space or the line break an `always` option asked for landed outside the function; PostCSS then trimmed it out of the value and into the raws of what followed, which left the value looking untouched to the next run, and the next run wrote another one. `--fix` grew the file by a character a run, for as long as it was asked, and reported nothing at all. Such a function is left alone now, warning and all, since the parentheses these options are about are not where the parser puts them. A closed call standing inside one is still read and fixed where it stands, and a bracket the file really leaves open never gets this far: PostCSS throws on one of those before any rule sees the declaration.
	- The `unit-case` rule no longer reads a bang flag standing in a value, or in the parameters of an at-rule, as part of the unit in front of it (see [#234](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/234)). PostCSS moves only the last `!important` of a declaration out of the value, so every flag written in front of it stays where it was, and `postcss-value-parser` reads `1px!important` as one word — whose unit the rule measured as `px!important`. Under `"upper"` the fix wrote the keyword in capitals along with the unit, so `a { b: 1px!important 2px!important; }` came out of `--fix` as `a { b: 1PX!IMPORTANT 2PX!important; }`, and nothing was reported; under `"lower"` the same reading rewrote a declaration whose units were already right, `a { b: 1px!IMPORTANT 2px!important; }` losing the spelling its flag was given. A unit ends where the flag begins now, so the keyword keeps the case the file gives it under either option, the warning names the unit rather than the unit and the flag together and ends where the unit ends, a Sass `!default` or `!global` stands the same way, and a Less at-variable such as `@variable: 10px!important` is read like any other declaration. A word whose whole unit is a flag, the `1!important` of `a { b: 1!important 1px!important; }`, carries no unit at all and is passed over. A unit no specification knows is recased as before: the rule asks where the unit ends, not whether it is one.
	- The `unit-case`, `color-hex-case`, `function-parentheses-*-inside` and `number-*` rules no longer read the text of an end-of-line comment as code (see [#271](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/271)). These six walk a value with `postcss-value-parser`, which has a node for a block comment and none for one opened by a double slash, so a block comment reached them as a node of its own and was passed over while an end-of-line comment reached them as ordinary words, calls and divs, and each worked on that text as it works on the value. `a { b: 1px // 2px` followed by `3px; }` came back from `--fix` with `// 2PX`, a hex colour written in such a comment was recased, a call spelled there had its parentheses spaced or broken, and a number written there lost a zero or gained one. A set of at-rule parameters carries such a comment the same way, and was read the same way — under `postcss-less` all along, and under `postcss-scss` since [#233](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/233) put `unit-case` on the copy the file spells and [#268](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/268) did the same for the two `number-*` rules. A node standing in that text is left alone now, and nothing is reported about it, while the code standing on either side of the comment is read and fixed as before.
		- `function-parentheses-newline-inside` did the worst of it, since the line breaks its `always` options wrote ended the comment early and turned the rest of its text into code the file is read by.
		- A call opened in the comment's text is left alone however far it reaches, parentheses and all: `a { b: f( 1 ) // g( 2` followed by `3 ); }` used to have the spacing of both its calls written, on the comment's line and on the line under it, and only the call in front of the comment is spaced now. What such a call gathered out of the code below the comment is walked as ever, so a unit, a colour, a number or a nested call standing there is read where it stands.
		- Where such a call was all a warning was ever about, the warning goes with it. `a { t: foo(1px // c) calc(` followed by `2px); }` drew warnings from both `function-parentheses-*-inside` rules about the parentheses of a `calc`, and Less itself reads that stylesheet as `foo(1px 2px)`: the whole of `calc(` stands inside the comment. The guards that hold a fix back where it would carry code into a comment — [#129](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/129), [#280](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/280) and [#281](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/281) — still stand in front of every fix that would. A call the parser closes the other way about — opened outside such a comment and closed on a parenthesis standing inside it — is read as ever, and the whitespace an `always` option asks for in front of that parenthesis is still written into the comment; that is [#320](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/320).
		- One line is read as the text of a comment that is none of it, and the six now say nothing about it where they used to report it and fix it. The scan that finds these comments takes a backslash for an ordinary character outside `url()` and outside a quoted string, so everything behind the escape of `a { b: a\//b 1PX; }` under `postcss-less` is a comment's text to it, where Less itself reads `a\/ / b 1PX` and the unit is code. The gap is one scan's and every reader of that scan has it (see [#321](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/321)).
- The warning was right and what came of it was not — a position off its mark, a fix discarded or refused where nothing was at risk:
	- The `value-list-comma-space-after` and `value-list-comma-newline-after` rules now fix a comma that opens the value (see [#134](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/134)). Such a comma is the value's first character, and the whitespace behind it belongs to the value like any other, but the boundary that keeps these rules off a comma standing in the property name was a character too wide and refused that one as well, so `a { prop: ,0; }` was reported and then left exactly as it was, however many times `--fix` was run. Every option of both rules is affected, so `never` now closes the whitespace up as `always` opens it, and a custom property is fixed like any other declaration. A comma standing in the property name is still reported and still left alone, since nothing these rules could write would reach it. The same comma in `value-list-comma-space-before` took more than a boundary moved, the whitespace at stake there standing outside the value, and is answered below.
	- The `value-list-comma-space-before` rule now fixes a comma that opens the value (see [#166](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/166)). The whitespace in front of such a comma is none of the value's: it is the text standing between the colon and the value, which PostCSS keeps in the declaration's `raws.between`, and no write to the value could reach it. So the problem was reported and the code left exactly as it was, however many times `--fix` was run — under `never` and `never-single-line` wherever a declaration is spelled with whitespace after its colon, and under `always` and `always-single-line` wherever it is not. The rule writes that text now, so `a { prop: ,0; }` is closed up under `never` and `a { prop:,0; }` opened to a single space under `always`, a line break standing after the colon collapsing into that space as one standing in the value always did, and a custom property is written like any other declaration while a comment between the colon and the value keeps its place. A comma standing behind an inline comment is still reported and still left alone, since the line break in that whitespace is what closes the comment. The whitespace after the colon is also what `declaration-colon-space-after` writes, so a configuration that asks one of the two rules for a space there and the other for none cannot be satisfied by any file: the rule standing later in the configuration writes last, and the other's warning is raised again by the next run over the output. Nothing runs away with it — a second `--fix` writes the same file as the first.
	- The `declaration-block-semicolon-newline-before` rule now writes its line break in front of the semicolon of a declaration carrying `!important` (see [#203](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/203)). The `always` options measured the whitespace in front of the semicolon, which stands behind the flag, and wrote the break to the end of the value, which ends before it, wherever the flag was spelled the plain way — PostCSS keeps a raw for ` !important` only where it is spelled some other way, and the rule wrote to the flag only where it found that raw. So the first run put the break in front of the flag, `a { color: pink !important; }` coming out as `a { color: pink\n !important; }`, the second put another one where the first belonged, and neither reported a thing, `--fix` claiming a clean pass twice on a file it was still changing. The break goes behind the flag now, whichever way the flag is spelled, and a comment standing on either side of it keeps its place.
	- The `selector-descendant-combinator-no-non-space` rule now spells a selector the way the file spells it in the text of a warning under `postcss-scss`. The text a warning quotes is read out of the copy that syntax rewrites every inline comment in, so `.foo\t// c\n( )\t.bar` was reported as `Unexpected "\t/* c*/\n( )"`, a text the file does not hold. It is read back out of the copy the file spells now, as the position it is reported at already was.
	- The `selector-pseudo-class-parentheses-space-inside` rule now reads a carriage return and a form feed as the line breaks they are (see [#173](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/173)). The `never` option spares the whitespace in front of the closing parenthesis of a multi-line argument list, but a list counted as multi-line only where it held a line feed, so `a:not( b\r, c )` had that space taken away while the same list broken with `\n` kept it. The three characters the syntaxes end a line on are read alike now; `\r\n` was spared all along, by its `\n`.
	- Every `*-newline-*` rule now reads a carriage return and a form feed as the line breaks they are (see [#196](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/196)). The checker they share asked for a line feed and took nothing else, so a file broken with a bare `\r` or a `\f` was reported for a break it already held — `.a\r, .b` for one — and the fix made it worse: most of the rules wrote a line feed in front of the break and left the pair standing, `a,\n\rb`, while `selector-list-comma-newline-before` and `block-opening-brace-newline-before`, whose fix goes in front of the indentation the break closes, grew the file by a line with every run of `--fix`, for ever. `function-comma-newline-before`, `function-comma-newline-after` and `declaration-block-semicolon-newline-before` rewrote the break into a line feed instead, and stop doing that too. The three characters every parser this plugin reads through ends a line on are taken alike now, `\r\n` as it always was. Sixteen rules are built on that checker: the four `selector-list-comma-newline-*` and `value-list-comma-newline-*`, the two `function-comma-newline-*` and `media-query-list-comma-newline-*`, `declaration-colon-newline-after`, the two `declaration-block-semicolon-newline-*`, the three `block-*-brace-newline-*`, `at-rule-name-newline-after` and `at-rule-semicolon-newline-after`.
	- The `block-opening-brace-newline-after`, `block-closing-brace-newline-after`, `declaration-colon-newline-after`, `declaration-block-semicolon-newline-after` and `media-query-list-comma-newline-after` rules now trim the whitespace in front of a bare carriage return or a form feed up to that break, the way they trim it in front of a line feed (see [#204](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/204)). Their fixers looked for the break themselves, with a line feed in their regular expressions, and wrote one in where they found none, so `a {  \rcolor: pink; }` came out of `--fix` as `a {\n  \rcolor: pink; }` — a stray line feed in front of the old break and the spaces it was reported for still standing — where its line-feed twin comes out as `a {\ncolor: pink; }`. The same reading told `block-opening-brace-newline-after` which breaks to take away under `never-multi-line` and `block-closing-brace-newline-after` which comment ends on the brace's line, and told `declaration-colon-newline-after` and `media-query-list-comma-newline-after` which comment stands on the colon's or the comma's own line: a block comment behind a form feed was read as one that does, the rules asking for a break behind it, and in the second rule an end-of-line `//` comment closed by a bare carriage return, or by a form feed under Sass, was read as one that does not, the fix pushing it onto a line of its own. All of it reads every line break now.
	- The `block-closing-brace-newline-before` rule now reads a carriage return and a form feed as the line breaks they are (see [#209](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/209)). It is the one rule of the before family that looks for the break itself rather than through the checker [#196](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/196) taught, and both places it looks asked for a line feed. Its fixer wrote one in wherever it found none, so `a { color: pink;  \r}` came out of `--fix` under `always` as `a { color: pink;\n  \r}` — a stray line feed in front of the old break and the spaces it was reported for still standing — where the line-feed twin of that file comes out as `a { color: pink;\n}`; `always-multi-line` did the same. And the condition that raises the warning read the block's final whitespace for a line feed, or for a Windows break, and for nothing else, so `a { color: pink;\r}` was reported for the break it already held and then given a second one. Both halves read every line break now, so `a { color: pink;  \r}` is trimmed to the break it already holds and `a { color: pink;\r}` is left alone, exactly as their line-feed twins are. Under `always-multi-line` that holds wherever the block counts as multi-line, which a line feed or a carriage return still decides on its own: a block whose only break is a form feed passes for a single line, and the option passes it over as it always did.
	- The `no-eol-whitespace` rule now trims every line of a comment when fixing, and not only the lines up to the first quotation mark in it (see [#67](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/67)). An apostrophe in the text of a comment, the one in `isn't` for instance, used to be taken for the opening quote of a string, and the lines after it were passed over — reported, but left as they were, however many times the fix was run.
	- The `string-quotes` rule no longer loses its bearings in a declaration's value or an at-rule's parameters holding a comment (see [#61](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/61), [#33](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/33)):
		- the reported position now points at the quote itself instead of drifting by the length of every comment standing in front of it;
		- the fix now changes the quote characters and nothing else, so every comment in the value survives it.
	- A `//` comment that a carriage return ends is now read as ended wherever the plugin asks whether a fix would land inside one. Sass reads that character as a line of its own, and Less turns one into a line feed before it parses anything, so a comment ends there in either language; every rule that declines a fix next to such a comment used to read the comment as running on, and declined where nothing was at risk.
	- A double slash the file spells no comment with is no longer read as one wherever the plugin asks whether a fix would land inside a comment (see [#225](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/225)). Plain CSS spells none that way, so `a { b: 1px//c` followed by `;` on the next line ends in code and the semicolon has a line to join, but the question was asked of the text alone, which cannot tell the two spellings apart, and nine rules refused a fix where nothing was at risk. The syntax is asked first now, as the four rules repaired alongside [#216](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/216) already ask it. Six of the nine are rules a file of plain CSS can reach — `declaration-block-semicolon-space-before`, `declaration-block-semicolon-newline-before`, `declaration-block-trailing-semicolon`, `function-parentheses-space-inside`, `function-parentheses-newline-inside` and `media-feature-parentheses-space-inside`, which read a value or a set of parameters. The other three — `block-opening-brace-space-before`, `block-opening-brace-newline-before` and `declaration-colon-space-before` — read a `raws.between`, and plain CSS puts no comment of that kind in one: it reads `a //c` as a selector rather than as a selector and a comment. The only double slash that can reach those three is the pair `*/` and `/*` spell between two block comments, which the reading already tells from a comment's own, so nothing they do changes. The guards themselves are as load-bearing as ever: under Sass and under Less a `//` comment does stand in a `raws.between`, and the fix is still held back there.
	- The `block-closing-brace-empty-line-before` rule's fix no longer writes a second empty line onto a block that is one break short of holding one (see [#245](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/245)). It put `context.newline` twice on the end of whatever stood there wherever the break already standing was neither a line feed nor a Windows pair, so `a {\rcolor: pink;\r}` came back as `a {\rcolor: pink;\r\n\n}` — two empty lines where one was asked for, spelled with a break the file does not use — and the next run was silent, so the file kept them. The break already standing is written twice over now, so `a {\rcolor: pink;\r}` comes back as `a {\rcolor: pink;\r\r}`; and where the whitespace holds none, the break the file ends its lines with is written instead, so `a {\rcolor: pink;}` comes back as `a {\rcolor: pink;\r\r}` as well. That break is read off the file, as [#267](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/267) has it below, and `context.newline` is written only where the file ends no line at all. The fix under `never` keeps that spelling too, writing the first break of a run in the whole run's place rather than `context.newline`, which knows a Windows pair and a line feed and no other break. A stray semicolon standing in that whitespace no longer moves the empty line either: it is written behind the semicolon rather than in front, and taken away from around it under `never`, where `a {\ncolor: pink;\n;\n}` used to draw a warning that no run of `--fix` could clear.
	- The `block-closing-brace-empty-line-before` rule's fix now spells its empty line the way the file spells its lines, wherever the break it copies stands (see [#267](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/267)). Where the whitespace in front of the closing brace held no break of its own, one was looked for in the whitespace the file keeps around the block and its tree — in front of a node, behind its name, between it and what follows that name, inside its block at the end, and in the semicolon a nested block may be followed by — and whitespace is not the only place a break stands. One standing in a selector, in a value, in a set of parameters or inside an `!important` was invisible to that reading, and `context.newline` went in instead, which reads a line feed and a Windows pair and knows neither of the two other breaks a stylesheet is written with: `a {b,\rc {color: pink;}}` and `a {color: pink !\rimportant;}` each came back carrying a line feed the file holds nowhere else. The file itself is read now, and the answer is the first break it spells outside its comments and its strings — a break inside either ends a line and says nothing about how the file ends its lines — so both come back spelled with the carriage return they are written with, and `context.newline` is left for the file that ends no line at all. A file spelling its lines two ways answers with its own first break rather than with its block's, so `a,\nb {\rcolor: pink; }` gains a line feed where it used to gain a carriage return.
	- The `function-parentheses-*-inside` rules now fix a closing parenthesis that neither reading of a line break moves into an inline comment (see [#132](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/132)). Both rules guard that fix by asking twice whether such a comment holds the parenthesis — once as the value stands, once as the fix would leave it — and each asking counted a set of breaks of its own: the first every break either syntax closes a comment on, the second only the breaks both of them do. Where a break of the difference stood between the double slash and the whitespace the fix writes over — a form feed, which Sass ends a comment on and Less does not — the two answers described two different languages: the first said the parenthesis was not inside a comment yet, the second that it would be inside one afterwards, so the problem was reported on every run and never fixed, though in either language on its own the parenthesis ends up exactly where it already stood. All four options of `function-parentheses-space-inside` reached it, and `never-multi-line` of `function-parentheses-newline-inside`. Both questions are put to each of the two readings now — the breaks both syntaxes read, and those with a form feed among them — and the fix is held back only where one and the same reading has the parenthesis outside a comment as the value stands and inside one once the fix has run. The two separators of Unicode, which the guard used to count as breaks for one of its two askings, are read by neither: a fix here writes over whitespace and the value parser counts as space only what stands below the blank, so a separator survives the fix and can never be what carries the parenthesis into a comment.
	- The `function-parentheses-newline-inside` rule now fixes the whitespace behind an opening parenthesis where an inline comment holds the first argument already (see [#281](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/281)). The `never-multi-line` option guards that fix by asking whether such a comment would hold the argument once the whitespace is gone, and never asked whether one holds it as the value stands — the two questions the closing half of the same rule has put since [#132](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/132), of which the opening half put one. So `a { t: foo(1px // c) calc( 2px` followed by a line break and `); }` under `postcss-less`, where the comment runs to that break and holds `2px` inside it before the fix as after it, was reported on every run and never written. Both questions are put now, and each of them to both readings of a line break — the breaks both syntaxes read, and those with a form feed among them — so the fix is held back only where one and the same reading has the argument outside a comment as the value stands and inside one once the fix has run. The one question also read a text that was neither of the two wherever the fix empties whitespace on both sides of a block comment, and let a breaking fix through there: `a { t: foo(1px // c) calc(` followed by a line break, `/*b*/ 2px); }`, came back as `a { t: foo(1px // c) calc(/*b*/2px); }` — the break that closed the comment gone, and everything behind the double slash commented out. That one is reported and left alone now: emptying the whitespace there would satisfy the option only by commenting the rest of the value out.

## [5.3.0] — 2026–08–09

### Added

- The `function-comma-newline-after` rule now has an additional `ignoreFunctions` option (see [#78](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/78)).
- The `function-comma-newline-before` rule now has an additional `ignoreFunctions` option (see [#81](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/81)).
- The `function-comma-space-after` rule now has an additional `ignoreFunctions` option (see [#82](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/82)).
- The `function-comma-space-before` rule now has an additional `ignoreFunctions` option (see [#83](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/83)).

## [5.2.1] — 2026–07–01

### Fixed

- The protocol in the repository's metadata URL now meets current requirements ([#79](https://github.com/stylelint-stylistic/stylelint-stylistic/pull/79)) ([@BowenMilner](https://github.com/BowenMilner)).

## [5.2.0] — 2026–05–20

### Added

- The `declaration-block-semicolon-newline-before` rule is now autofixable.

### Fixed

- An exception for an empty custom property value has been added to the `declaration-block-semicolon-newline-before` and `declaration-colon-space-after` rules: the `--custom-prop: ;` and `--custom-prop:;` variants are now considered valid (see [#50](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/50)).

## [5.1.0] — 2026–03–28

### Added

- The `no-multiple-whitespaces` rule, which disallows multiple whitespaces between property values and function arguments.

### Fixed

- The dependencies have now been updated to versions that include security fixes.

## [5.0.1] — 2026–01–22

### Fixed

- The `selector-pseudo-class-parentheses-space-inside` rule no longer triggers false positives in multiline pseudo-classes.

## [5.0.0] — 2026–01–15

### Changed

- The plugin now requires:
	- `stylelint` version `17.0.0` or higher
	- `node.js` version `20.19.0` or higher

## [4.0.1] — 2026–01–15

### Fixed

- Multiline pseudos are now aligned correctly with the `@stylistic/indentation` rule.

	**Before**:

	```css
	.foo:where(
	:not(
	    .bar,
	    .baz
	)
	) {}
	```

	**Now**:

	```css
	.foo:where(
	  :not(
	    .bar,
	    .baz
	  )
	) {}
	```

## [4.0.0] — 2025–07–22

### Changed

- The plugin now requires `stylelint` version `16.22.0` or higher.

## [3.1.3] — 2025–06–25

### Fixed

- `stylelint` has been moved from `dependencies` to `devDependencies`. This may potentially fix some errors (see [Stylelint's documentation regarding `peerDependencies`](https://stylelint.io/developer-guide/plugins#peer-dependencies) and [PRs that explain the motivation behind this decision](https://github.com/stylelint/stylelint/issues/2812)).
- `postcss` has been moved from `devDependencies` to `dependencies`. This fixes the “Cannot find package `postcss`” and “Named export `Input` not found” error in some environments.

## [3.1.2] — 2025–02–05

### Fixed

- An explicit end position is now passed to all `report` calls. \
	The `report` calls no longer receive the `line` argument, which was [deprecated](https://github.com/stylelint/stylelint/pull/8244) in `stylelint@16.13.0`. \
	Previously, attempts to update `stylelint` to `16.13.0` version resulted in multiple DeprecationWarning messages ([#53](https://github.com/stylelint-stylistic/stylelint-stylistic/pull/53)) ([@MorevM](https://github.com/MorevM)).
- Added an exception to the `declaration-block-semicolon-space-before` rule for an empty value of a custom property: now both `--custom-prop: ;` and `--custom-prop:;` are considered valid even with the `never` and `never-single-line` options. \
  You can find a detailed explanation in [the original issue](https://github.com/stylelint-stylistic/stylelint-stylistic/issues/50) ([#51](https://github.com/stylelint-stylistic/stylelint-stylistic/pull/51)) ([@MorevM](https://github.com/MorevM)).
- Fixed behavior of `baseIndentLevel` option of `indentation` rule when used in non-CSS files (e.g. when using `postcss-html` syntax) ([#47](https://github.com/stylelint-stylistic/stylelint-stylistic/pull/47)) ([@net-solution](https://github.com/net-solution)).
- Fixed removing the starting indentation along with the blank line in the `no-empty-first-rule` rule ([#47](https://github.com/stylelint-stylistic/stylelint-stylistic/pull/47)) ([@net-solution](https://github.com/net-solution)).

## [3.1.1] — 2024–10–04

### Fixed

- Indentation checking for property values that use dynamic expressions when using `postcss-styled-syntax` is now disabled ([#44](https://github.com/stylelint-stylistic/stylelint-stylistic/pull/44)) ([@MorevM](https://github.com/MorevM)).

## [3.1.0] — 2024–09–23

### Added

- The `messageArgs` to 16 rules for custom message arguments. See [stylelint documentation](https://stylelint.io/user-guide/configure/#message) for details.

### Fixed

- Calculation of indentation using `postcss-styled-syntax` custom syntax ([#41](https://github.com/stylelint-stylistic/stylelint-stylistic/pull/41)) ([@MorevM](https://github.com/MorevM)).

## [3.0.1] — 2024–08–18

### Fixed

- The `context.fix`, which is deprecated in `stylelint@16.8.2`, is no longer used. Previously, attempts to update `stylelint` to `16.8.2` version resulted in multiple DeprecationWarning messages ([#37](https://github.com/stylelint-stylistic/stylelint-stylistic/pull/37)).

## [3.0.0] — 2024–07–30

### Changed

- The plugin now requires `stylelint` version `16.8.0` or higher.

## [2.1.3] — 2024–07–29

### Fixed

- Dependencies are now updated, which fixes test fails ([#29](https://github.com/stylelint-stylistic/stylelint-stylistic/pull/29)) ([@ybiquitous](https://github.com/ybiquitous)).

## [2.1.2] — 2024–04–28

### Fixed

- Autofixing of `@charset` name by `string-quotes` rule ([#26](https://github.com/stylelint-stylistic/stylelint-stylistic/pull/26)) ([@Mouvedia](https://github.com/Mouvedia)).

## [2.1.1] — 2024–03–31

### Fixed

- `block-closing-brace-empty-line-before` with except: ["after-closing-brace"] false negatives for CSS Nesting ([#22](https://github.com/stylelint-stylistic/stylelint-stylistic/pull/22)) ([@firefoxic](https://github.com/firefoxic)).
- `named-grid-areas-alignment` for single-line input ([#21](https://github.com/stylelint-stylistic/stylelint-stylistic/pull/21)) ([@MorevM](https://github.com/MorevM)).

## [2.1.0] — 2024–02–18

### Added

- `named-grid-areas-alignment` rule ([#16](https://github.com/stylelint-stylistic/stylelint-stylistic/pull/16)) ([@MorevM](https://github.com/MorevM)).

## [2.0.0] — 2023–12–20

### Changed

- The repository renamed to `stylelint-stylistic` and moved to the organization of the same name.
- The plugin is published in `npm` under the new name `@stylistic/stylelint-plugin`. Therefore, you should:
	- change the former plugin name `stylelint-codeguide` to the new one in the config,
	- remove the old `stylelint-codeguide` package from dependencies
	- install the new `@stylistic/stylelint-plugin` package.
- The namespace of plugin rules has been renamed. Therefore, you should change the rules prefix from `codeguide/` to `@stylistic/` in the config.

## [1.0.2] — 2023–12–12

### Updated

- **Stylelint** to version `16.0.2`, which fixes the use of plugins in Windows.

## [1.0.1] — 2023–12–10

### Fixed

- Dependency definition.

## [1.0.0] — 2023–12–08

### Changed

- The required version of **Stylelint** is now at least `v16.0.1`.
- The required version of **Node.js** is now **LTS** at least `v18.12`, or at least `v20.9`.
- The plugin is now converted to a pure ES module.

## [0.3.2] — 2023–10–19

### Updated

- Stylelint to `^15.11.0`.
- Node.js to `>=18.16`.

## [0.3.1] — 2023–10–13

No meaningful changes.

## [0.3.0] — 2023–10–13

### Changed

- Along with updating the plugin to this release, Stylelint needs to be updated to `v15.10.3` as well.

### Updated

- All code according to `stylelint@15.10.3`.
- Peer dependency — `stylelint@15.10.3`.

## [0.2.2] — 2023–09–14

### Fixed

- The path to the internal module.

## [0.2.1] — 2023–07–08

### Fixed

- Any LTS version of Node.js on the 18th branch is now required (i.e. at least `18.12.0`), not the latest.
- Any version of Stylelint on the 15th branch is now required (i.e. at least `15.0.0`), not the latest.

## [0.2.0] — 2023–07–01

### Updated

- Peer dependency Stylelint to `15.9.0`.

## [0.1.5] — 2023–07–01

### Fixed

- All paths to docs.

## [0.1.4] — 2023–05–20

### Changed

- Reorganized `package.json`.

## [0.1.3] — 2023–03–28

### Removed

- The `deprecated` flag from the `meta` of each rule.

## [0.1.2] — 2023–03–28

### Fixed

- `Error [ERR_REQUIRE_ESM]: require() of ES Module`.

## [0.1.1] — 2023–03–28

### Added

- Babel until Stylelint is converted to ES Modules.

## [0.1.0] — 2023–03–28

### Added

- `at-rule-name-case` rule.
- `at-rule-name-newline-after` rule.
- `at-rule-name-space-after` rule.
- `at-rule-semicolon-newline-after` rule.
- `at-rule-semicolon-space-before` rule.
- `block-closing-brace-empty-line-before` rule.
- `block-closing-brace-newline-after` rule.
- `block-closing-brace-newline-before` rule.
- `block-closing-brace-space-after` rule.
- `block-closing-brace-space-before` rule.
- `block-opening-brace-newline-after` rule.
- `block-opening-brace-newline-before` rule.
- `block-opening-brace-space-after` rule.
- `block-opening-brace-space-before` rule.
- `color-hex-case` rule.
- `declaration-bang-space-after` rule.
- `declaration-bang-space-before` rule.
- `declaration-block-semicolon-newline-after` rule.
- `declaration-block-semicolon-newline-before` rule.
- `declaration-block-semicolon-space-after` rule.
- `declaration-block-semicolon-space-before` rule.
- `declaration-block-trailing-semicolon` rule.
- `declaration-colon-newline-after` rule.
- `declaration-colon-space-after` rule.
- `declaration-colon-space-before` rule.
- `function-comma-newline-after` rule.
- `function-comma-newline-before` rule.
- `function-comma-space-after` rule.
- `function-comma-space-before` rule.
- `function-max-empty-lines` rule.
- `function-parentheses-newline-inside` rule.
- `function-parentheses-space-inside` rule.
- `function-whitespace-after` rule.
- `indentation` rule.
- `linebreaks` rule.
- `max-empty-lines` rule.
- `max-line-length` rule.
- `media-feature-colon-space-after` rule.
- `media-feature-colon-space-before` rule.
- `media-feature-name-case` rule.
- `media-feature-parentheses-space-inside` rule.
- `media-feature-range-operator-space-after` rule.
- `media-feature-range-operator-space-before` rule.
- `media-query-list-comma-newline-after` rule.
- `media-query-list-comma-newline-before` rule.
- `media-query-list-comma-space-after` rule.
- `media-query-list-comma-space-before` rule.
- `no-empty-first-line` rule.
- `no-eol-whitespace` rule.
- `no-extra-semicolons` rule.
- `no-missing-end-of-source-newline` rule.
- `number-leading-zero` rule.
- `number-no-trailing-zeros` rule.
- `property-case` rule.
- `selector-attribute-brackets-space-inside` rule.
- `selector-attribute-operator-space-after` rule.
- `selector-attribute-operator-space-before` rule.
- `selector-combinator-space-after` rule.
- `selector-combinator-space-before` rule.
- `selector-descendant-combinator-no-non-space` rule.
- `selector-list-comma-newline-after` rule.
- `selector-list-comma-newline-before` rule.
- `selector-list-comma-space-after` rule.
- `selector-list-comma-space-before` rule.
- `selector-max-empty-lines` rule.
- `selector-pseudo-class-case` rule.
- `selector-pseudo-class-parentheses-space-inside` rule.
- `selector-pseudo-element-case` rule.
- `string-quotes` rule.
- `unicode-bom` rule.
- `unit-case` rule.
- `value-list-comma-newline-after` rule.
- `value-list-comma-newline-before` rule.
- `value-list-comma-space-after` rule.
- `value-list-comma-space-before` rule.
- `value-list-max-empty-lines` rule.

[Unreleased]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v5.3.0...HEAD
[5.3.0]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v5.2.1...v5.3.0
[5.2.1]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v5.2.0...v5.2.1
[5.2.0]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v5.1.0...v5.2.0
[5.1.0]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v5.0.1...v5.1.0
[5.0.1]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v5.0.0...v5.0.1
[5.0.0]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v4.0.1...v5.0.0
[4.0.1]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v4.0.0...v4.0.1
[4.0.0]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v3.1.3...v4.0.0
[3.1.3]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v3.1.2...v3.1.3
[3.1.2]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v3.1.1...v3.1.2
[3.1.1]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v3.1.0...v3.1.1
[3.1.0]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v3.0.1...v3.1.0
[3.0.1]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v3.0.0...v3.0.1
[3.0.0]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v2.1.3...v3.0.0
[2.1.3]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v2.1.2...v2.1.3
[2.1.2]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v2.1.1...v2.1.2
[2.1.1]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v2.1.0...v2.1.1
[2.1.0]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v2.0.0...v2.1.0
[2.0.0]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v1.0.2...v2.0.0
[1.0.2]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v1.0.1...v1.0.2
[1.0.1]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.3.2...v1.0.0
[0.3.2]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.3.1...v0.3.2
[0.3.1]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.2.2...v0.3.1
[0.3.0]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.2.2...v0.3.0
[0.2.2]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.2.1...v0.2.2
[0.2.1]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.1.5...v0.2.0
[0.1.5]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.1.4...v0.1.5
[0.1.4]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.1.3...v0.1.4
[0.1.3]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.1.2...v0.1.3
[0.1.2]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.1.1...v0.1.2
[0.1.1]: https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/stylelint-stylistic/stylelint-stylistic/releases/tag/v0.1.0
