/** The harness's own expressions. */

/** A line break as `lintPostcssResult.mjs` reads one to fill `context.newline`. */
const BREAK_AS_STYLELINT_READS_IT = /\r?\n/u

export { BREAK_AS_STYLELINT_READS_IT }
