# Enforce Unix newlines
* text=auto eol=lf

# A unified diff puts its context marker in front of the indentation the patched
# file already has, which every whitespace check reads as a space before a tab.
patches/*.patch -whitespace
